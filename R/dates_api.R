# OpenFEC
#
# This application programming interface (API) allows you to explore the way candidates and committees fund their campaigns.    The Federal Election Commission (FEC) API is a RESTful web service supporting full-text and field-specific searches on FEC data. [Bulk downloads](https://www.fec.gov/data/advanced/?tab=bulk-data) are available on the current site. Information is tied to the underlying forms by file ID and image ID. Data is updated nightly.    There are a lot of data, and a good place to start is to use search to find interesting candidates and committees. Then, you can use their IDs to find report or line item details with the other endpoints. If you are interested in individual donors, check out contributor information in the `/schedule_a/` endpoints.    <b class=\"body\" id=\"getting_started_head\">Getting started with the openFEC API</b><br>    If you would like to use the FEC's API programmatically, you can sign up for your own API key using our form. Alternatively, you can still try out our API without an API key by using the web interface and using DEMO_KEY. Note that when you use the openFEC API you are subject to the [Terms of Service](https://github.com/fecgov/FEC/blob/master/TERMS-OF-SERVICE.md) and [Acceptable Use policy](https://github.com/fecgov/FEC/blob/master/ACCEPTABLE-USE-POLICY.md).    Signing up for an API key will enable you to place up to 1,000 calls an hour. Each call is limited to 100 results per page. You can email questions, comments or a request to get a key for 7,200 calls an hour (120 calls per minute) to <a href=\"mailto:APIinfo@fec.gov\">APIinfo@fec.gov</a>. You can also ask questions and discuss the data in a community led [group](https://groups.google.com/forum/#!forum/fec-data).    The model definitions and schema are available at [/swagger](/swagger/). This is useful for making wrappers and exploring the data.    A few restrictions limit the way you can use FEC data. For example, you can’t use contributor lists for commercial purposes or to solicit donations. [Learn more here](https://www.fec.gov/updates/sale-or-use-contributor-information/).    [Inspect our source code](https://github.com/fecgov/openFEC). We welcome issues and pull requests!    <p><br></p> <h2 class=\"title\" id=\"signup_head\">Sign up for an API key</h2> <div id=\"apidatagov_signup\">Loading signup form...</div>
#
# The version of the OpenAPI document: 1.0
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Dates operations
#' @description openapi.Dates
#' @format An \code{R6Class} generator object
#' @field apiClient Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ CalendarDatesExportGet } \emph{  }
#'  Returns CSV or ICS for downloading directly into calendar applications like Google, Outlook or other applications.  Combines the election and reporting dates with Commission meetings, conferences, outreach, Advisory Opinions, rules, litigation dates and other events into one calendar.  State filtering now applies to elections, reports and reporting periods.  Presidential pre-primary report due dates are not shown on even years. Filers generally opt to file monthly rather than submit over 50 pre-primary election reports. All reporting deadlines are available at /reporting-dates/ for reference.  This is [the sql function](https://github.com/fecgov/openFEC/blob/develop/data/migrations/V40__omnibus_dates.sql) that creates the calendar.  
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } max.start.date character
#' \item \emph{ @param } calendar.category.id list( integer )
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } description list( character )
#' \item \emph{ @param } min.end.date character
#' \item \emph{ @param } min.start.date character
#' \item \emph{ @param } max.end.date character
#' \item \emph{ @param } renderer Enum < [ics, csv] > 
#' \item \emph{ @param } sort character
#' \item \emph{ @param } summary list( character )
#' \item \emph{ @param } event.id integer
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{CalendarDatePage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : CalendarDatePage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CalendarDatesGet } \emph{  }
#'  Combines the election and reporting dates with Commission meetings, conferences, outreach, Advisory Opinions, rules, litigation dates and other events into one calendar.  State and report type filtering is no longer available. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } max.start.date character
#' \item \emph{ @param } calendar.category.id list( integer )
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } description list( character )
#' \item \emph{ @param } min.end.date character
#' \item \emph{ @param } min.start.date character
#' \item \emph{ @param } max.end.date character
#' \item \emph{ @param } sort character
#' \item \emph{ @param } page integer
#' \item \emph{ @param } summary list( character )
#' \item \emph{ @param } event.id integer
#' \item \emph{ @returnType } \link{CalendarDatePage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : CalendarDatePage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ElectionDatesGet } \emph{  }
#'  FEC election dates since 1995. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } min.update.date character
#' \item \emph{ @param } min.primary.general.date character
#' \item \emph{ @param } min.election.date character
#' \item \emph{ @param } max.primary.general.date character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } election.district list( character )
#' \item \emph{ @param } max.election.date character
#' \item \emph{ @param } max.update.date character
#' \item \emph{ @param } sort character
#' \item \emph{ @param } election.type.id list( character )
#' \item \emph{ @param } max.create.date character
#' \item \emph{ @param } min.create.date character
#' \item \emph{ @param } election.state list( character )
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } election.party list( character )
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } office.sought Enum < [H, S, P] > 
#' \item \emph{ @param } page integer
#' \item \emph{ @param } election.year list( character )
#' \item \emph{ @returnType } \link{InlineResponseDefault} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : InlineResponseDefault 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ ReportingDatesGet } \emph{  }
#'  FEC election dates since 1995. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } min.update.date character
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } max.due.date character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } max.update.date character
#' \item \emph{ @param } max.create.date character
#' \item \emph{ @param } sort character
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } min.create.date character
#' \item \emph{ @param } report.type list( character )
#' \item \emph{ @param } min.due.date character
#' \item \emph{ @param } page integer
#' \item \emph{ @param } report.year list( integer )
#' \item \emph{ @returnType } \link{InlineResponseDefault2} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : InlineResponseDefault2 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  CalendarDatesExportGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.max.start.date <- 'max.start.date_example' # character |  The maximum start date.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.calendar.category.id <- [56] # array[integer] |  Each type of event has a calendar category with an integer id. Options are: Open Meetings: 32, Executive Sessions: 39, Public Hearings: 40, Conferences: 33, Roundtables: 34, Election Dates: 36, Federal Holidays: 37, FEA Periods: 38, Commission Meetings: 20, Reporting Deadlines: 21, Conferences and Outreach: 22, AOs and Rules: 23, Other: 24, Quarterly: 25, Monthly: 26, Pre and Post-Elections: 27, EC Periods:28, and IE Periods: 29 
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.description <- ['description_example'] # array[character] | Brief description of event
#' var.min.end.date <- 'min.end.date_example' # character |  The minimum end date.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.min.start.date <- 'min.start.date_example' # character |  The minimum start date.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.max.end.date <- 'max.end.date_example' # character |  The maximum end date.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.renderer <- 'ics' # character | 
#' var.sort <- '-start_date' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.summary <- ['summary_example'] # array[character] | Longer description of event
#' var.event.id <- 56 # integer | An unique ID for an event. Useful for downloading a single event to your calendar. This ID is not a permanent, persistent ID.
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- DatesApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$CalendarDatesExportGet(var.api.key, sort.hide.null=var.sort.hide.null, per.page=var.per.page, sort.nulls.last=var.sort.nulls.last, max.start.date=var.max.start.date, calendar.category.id=var.calendar.category.id, sort.null.only=var.sort.null.only, description=var.description, min.end.date=var.min.end.date, min.start.date=var.min.start.date, max.end.date=var.max.end.date, renderer=var.renderer, sort=var.sort, summary=var.summary, event.id=var.event.id, page=var.page)
#'
#'
#' ####################  CalendarDatesGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.max.start.date <- 'max.start.date_example' # character |  The maximum start date.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.calendar.category.id <- [56] # array[integer] |  Each type of event has a calendar category with an integer id. Options are: Open Meetings: 32, Executive Sessions: 39, Public Hearings: 40, Conferences: 33, Roundtables: 34, Election Dates: 36, Federal Holidays: 37, FEA Periods: 38, Commission Meetings: 20, Reporting Deadlines: 21, Conferences and Outreach: 22, AOs and Rules: 23, Other: 24, Quarterly: 25, Monthly: 26, Pre and Post-Elections: 27, EC Periods:28, and IE Periods: 29 
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.description <- ['description_example'] # array[character] | Brief description of event
#' var.min.end.date <- 'min.end.date_example' # character |  The minimum end date.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.min.start.date <- 'min.start.date_example' # character |  The minimum start date.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.max.end.date <- 'max.end.date_example' # character |  The maximum end date.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.sort <- '-start_date' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#' var.summary <- ['summary_example'] # array[character] | Longer description of event
#' var.event.id <- 56 # integer | An unique ID for an event. Useful for downloading a single event to your calendar. This ID is not a permanent, persistent ID.
#'
#' api.instance <- DatesApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$CalendarDatesGet(var.api.key, sort.hide.null=var.sort.hide.null, per.page=var.per.page, sort.nulls.last=var.sort.nulls.last, max.start.date=var.max.start.date, calendar.category.id=var.calendar.category.id, sort.null.only=var.sort.null.only, description=var.description, min.end.date=var.min.end.date, min.start.date=var.min.start.date, max.end.date=var.max.end.date, sort=var.sort, page=var.page, summary=var.summary, event.id=var.event.id)
#'
#'
#' ####################  ElectionDatesGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.min.update.date <- 'min.update.date_example' # character |  The minimum date this record was last updated.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.min.primary.general.date <- 'min.primary.general.date_example' # character |  The minimum date of primary or general election.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.min.election.date <- 'min.election.date_example' # character |  The minimum date of election. 
#' var.max.primary.general.date <- 'max.primary.general.date_example' # character |  The maximum date of primary or general election.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.election.district <- ['election.district_example'] # array[character] |  House district of the office sought, if applicable. 
#' var.max.election.date <- 'max.election.date_example' # character |  The maximum date of election. 
#' var.max.update.date <- 'max.update.date_example' # character |  The maximum date this record was last updated.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.sort <- '-election_date' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.election.type.id <- ['election.type.id_example'] # array[character] |  Election type id 
#' var.max.create.date <- 'max.create.date_example' # character |  The maximum date this record was added to the system.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.min.create.date <- 'min.create.date_example' # character |  The minimum date this record was added to the system.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.election.state <- ['election.state_example'] # array[character] |  State or territory of the office sought. 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.election.party <- ['election.party_example'] # array[character] |  Party, if applicable. 
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.office.sought <- ['office.sought_example'] # array[character] |  House, Senate or presidential office. 
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#' var.election.year <- ['election.year_example'] # array[character] | Year of election
#'
#' api.instance <- DatesApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$ElectionDatesGet(var.api.key, min.update.date=var.min.update.date, min.primary.general.date=var.min.primary.general.date, min.election.date=var.min.election.date, max.primary.general.date=var.max.primary.general.date, per.page=var.per.page, election.district=var.election.district, max.election.date=var.max.election.date, max.update.date=var.max.update.date, sort=var.sort, election.type.id=var.election.type.id, max.create.date=var.max.create.date, min.create.date=var.min.create.date, election.state=var.election.state, sort.hide.null=var.sort.hide.null, sort.nulls.last=var.sort.nulls.last, election.party=var.election.party, sort.null.only=var.sort.null.only, office.sought=var.office.sought, page=var.page, election.year=var.election.year)
#'
#'
#' ####################  ReportingDatesGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.min.update.date <- 'min.update.date_example' # character |  The minimum date this record was last updated.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.max.due.date <- 'max.due.date_example' # character |  The maximum date the report is due.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.max.update.date <- 'max.update.date_example' # character |  The maximum date this record was last updated.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.max.create.date <- 'max.create.date_example' # character |  The maximum date this record was added to the system.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.sort <- '-due_date' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.min.create.date <- 'min.create.date_example' # character |  The minimum date this record was added to the system.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.report.type <- ['report.type_example'] # array[character] | Name of report where the underlying data comes from:     - 10D Pre-Election     - 10G Pre-General     - 10P Pre-Primary     - 10R Pre-Run-Off     - 10S Pre-Special     - 12C Pre-Convention     - 12G Pre-General     - 12P Pre-Primary     - 12R Pre-Run-Off     - 12S Pre-Special     - 30D Post-Election     - 30G Post-General     - 30P Post-Primary     - 30R Post-Run-Off     - 30S Post-Special     - 60D Post-Convention     - M1  January Monthly     - M10 October Monthly     - M11 November Monthly     - M12 December Monthly     - M2  February Monthly     - M3  March Monthly     - M4  April Monthly     - M5  May Monthly     - M6  June Monthly     - M7  July Monthly     - M8  August Monthly     - M9  September Monthly     - MY  Mid-Year Report     - Q1  April Quarterly     - Q2  July Quarterly     - Q3  October Quarterly     - TER Termination Report     - YE  Year-End     - ADJ COMP ADJUST AMEND     - CA  COMPREHENSIVE AMEND     - 90S Post Inaugural Supplement     - 90D Post Inaugural     - 48  48 Hour Notification     - 24  24 Hour Notification     - M7S July Monthly/Semi-Annual     - MSA Monthly Semi-Annual (MY)     - MYS Monthly Year End/Semi-Annual     - Q2S July Quarterly/Semi-Annual     - QSA Quarterly Semi-Annual (MY)     - QYS Quarterly Year End/Semi-Annual     - QYE Quarterly Semi-Annual (YE)     - QMS Quarterly Mid-Year/ Semi-Annual     - MSY Monthly Semi-Annual (YE) 
#' var.min.due.date <- 'min.due.date_example' # character |  The minimum date the report is due.(MM/DD/YYYY or YYYY-MM-DD) 
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#' var.report.year <- [56] # array[integer] |  Forms with coverage date -      year from the coverage ending date. Forms without coverage date -      year from the receipt date. 
#'
#' api.instance <- DatesApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$ReportingDatesGet(var.api.key, min.update.date=var.min.update.date, sort.hide.null=var.sort.hide.null, max.due.date=var.max.due.date, per.page=var.per.page, sort.nulls.last=var.sort.nulls.last, max.update.date=var.max.update.date, max.create.date=var.max.create.date, sort=var.sort, sort.null.only=var.sort.null.only, min.create.date=var.min.create.date, report.type=var.report.type, min.due.date=var.min.due.date, page=var.page, report.year=var.report.year)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
DatesApi <- R6::R6Class(
  'DatesApi',
  public = list(
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    CalendarDatesExportGet = function(api.key, sort.hide.null=FALSE, per.page=20, sort.nulls.last=FALSE, max.start.date=NULL, calendar.category.id=NULL, sort.null.only=FALSE, description=NULL, min.end.date=NULL, min.start.date=NULL, max.end.date=NULL, renderer='ics', sort='-start_date', summary=NULL, event.id=NULL, page=1, ...){
      apiResponse <- self$CalendarDatesExportGetWithHttpInfo(api.key, sort.hide.null, per.page, sort.nulls.last, max.start.date, calendar.category.id, sort.null.only, description, min.end.date, min.start.date, max.end.date, renderer, sort, summary, event.id, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    CalendarDatesExportGetWithHttpInfo = function(api.key, sort.hide.null=FALSE, per.page=20, sort.nulls.last=FALSE, max.start.date=NULL, calendar.category.id=NULL, sort.null.only=FALSE, description=NULL, min.end.date=NULL, min.start.date=NULL, max.end.date=NULL, renderer='ics', sort='-start_date', summary=NULL, event.id=NULL, page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['per_page'] <- per.page

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['max_start_date'] <- max.start.date

      queryParams['calendar_category_id'] <- calendar.category.id

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['description'] <- description

      queryParams['min_end_date'] <- min.end.date

      queryParams['min_start_date'] <- min.start.date

      queryParams['max_end_date'] <- max.end.date

      queryParams['renderer'] <- renderer

      queryParams['sort'] <- sort

      queryParams['summary'] <- summary

      queryParams['event_id'] <- event.id

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/calendar-dates/export/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "CalendarDatePage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    CalendarDatesGet = function(api.key, sort.hide.null=FALSE, per.page=20, sort.nulls.last=FALSE, max.start.date=NULL, calendar.category.id=NULL, sort.null.only=FALSE, description=NULL, min.end.date=NULL, min.start.date=NULL, max.end.date=NULL, sort='-start_date', page=1, summary=NULL, event.id=NULL, ...){
      apiResponse <- self$CalendarDatesGetWithHttpInfo(api.key, sort.hide.null, per.page, sort.nulls.last, max.start.date, calendar.category.id, sort.null.only, description, min.end.date, min.start.date, max.end.date, sort, page, summary, event.id, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    CalendarDatesGetWithHttpInfo = function(api.key, sort.hide.null=FALSE, per.page=20, sort.nulls.last=FALSE, max.start.date=NULL, calendar.category.id=NULL, sort.null.only=FALSE, description=NULL, min.end.date=NULL, min.start.date=NULL, max.end.date=NULL, sort='-start_date', page=1, summary=NULL, event.id=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['per_page'] <- per.page

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['max_start_date'] <- max.start.date

      queryParams['calendar_category_id'] <- calendar.category.id

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['description'] <- description

      queryParams['min_end_date'] <- min.end.date

      queryParams['min_start_date'] <- min.start.date

      queryParams['max_end_date'] <- max.end.date

      queryParams['sort'] <- sort

      queryParams['page'] <- page

      queryParams['summary'] <- summary

      queryParams['event_id'] <- event.id

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/calendar-dates/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "CalendarDatePage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    ElectionDatesGet = function(api.key, min.update.date=NULL, min.primary.general.date=NULL, min.election.date=NULL, max.primary.general.date=NULL, per.page=20, election.district=NULL, max.election.date=NULL, max.update.date=NULL, sort='-election_date', election.type.id=NULL, max.create.date=NULL, min.create.date=NULL, election.state=NULL, sort.hide.null=FALSE, sort.nulls.last=FALSE, election.party=NULL, sort.null.only=FALSE, office.sought=NULL, page=1, election.year=NULL, ...){
      apiResponse <- self$ElectionDatesGetWithHttpInfo(api.key, min.update.date, min.primary.general.date, min.election.date, max.primary.general.date, per.page, election.district, max.election.date, max.update.date, sort, election.type.id, max.create.date, min.create.date, election.state, sort.hide.null, sort.nulls.last, election.party, sort.null.only, office.sought, page, election.year, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    ElectionDatesGetWithHttpInfo = function(api.key, min.update.date=NULL, min.primary.general.date=NULL, min.election.date=NULL, max.primary.general.date=NULL, per.page=20, election.district=NULL, max.election.date=NULL, max.update.date=NULL, sort='-election_date', election.type.id=NULL, max.create.date=NULL, min.create.date=NULL, election.state=NULL, sort.hide.null=FALSE, sort.nulls.last=FALSE, election.party=NULL, sort.null.only=FALSE, office.sought=NULL, page=1, election.year=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['min_update_date'] <- min.update.date

      queryParams['min_primary_general_date'] <- min.primary.general.date

      queryParams['min_election_date'] <- min.election.date

      queryParams['max_primary_general_date'] <- max.primary.general.date

      queryParams['per_page'] <- per.page

      queryParams['election_district'] <- election.district

      queryParams['max_election_date'] <- max.election.date

      queryParams['max_update_date'] <- max.update.date

      queryParams['sort'] <- sort

      queryParams['election_type_id'] <- election.type.id

      queryParams['max_create_date'] <- max.create.date

      queryParams['min_create_date'] <- min.create.date

      queryParams['election_state'] <- election.state

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['election_party'] <- election.party

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['office_sought'] <- office.sought

      queryParams['page'] <- page

      queryParams['election_year'] <- election.year

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/election-dates/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "InlineResponseDefault", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    ReportingDatesGet = function(api.key, min.update.date=NULL, sort.hide.null=FALSE, max.due.date=NULL, per.page=20, sort.nulls.last=FALSE, max.update.date=NULL, max.create.date=NULL, sort='-due_date', sort.null.only=FALSE, min.create.date=NULL, report.type=NULL, min.due.date=NULL, page=1, report.year=NULL, ...){
      apiResponse <- self$ReportingDatesGetWithHttpInfo(api.key, min.update.date, sort.hide.null, max.due.date, per.page, sort.nulls.last, max.update.date, max.create.date, sort, sort.null.only, min.create.date, report.type, min.due.date, page, report.year, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    ReportingDatesGetWithHttpInfo = function(api.key, min.update.date=NULL, sort.hide.null=FALSE, max.due.date=NULL, per.page=20, sort.nulls.last=FALSE, max.update.date=NULL, max.create.date=NULL, sort='-due_date', sort.null.only=FALSE, min.create.date=NULL, report.type=NULL, min.due.date=NULL, page=1, report.year=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['min_update_date'] <- min.update.date

      queryParams['api_key'] <- api.key

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['max_due_date'] <- max.due.date

      queryParams['per_page'] <- per.page

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['max_update_date'] <- max.update.date

      queryParams['max_create_date'] <- max.create.date

      queryParams['sort'] <- sort

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['min_create_date'] <- min.create.date

      queryParams['report_type'] <- report.type

      queryParams['min_due_date'] <- min.due.date

      queryParams['page'] <- page

      queryParams['report_year'] <- report.year

      body <- NULL
      urlPath <- "/reporting-dates/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "InlineResponseDefault2", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    }
  )
)
