# OpenFEC
#
# This application programming interface (API) allows you to explore the way candidates and committees fund their campaigns.    The Federal Election Commission (FEC) API is a RESTful web service supporting full-text and field-specific searches on FEC data. [Bulk downloads](https://www.fec.gov/data/advanced/?tab=bulk-data) are available on the current site. Information is tied to the underlying forms by file ID and image ID. Data is updated nightly.    There are a lot of data, and a good place to start is to use search to find interesting candidates and committees. Then, you can use their IDs to find report or line item details with the other endpoints. If you are interested in individual donors, check out contributor information in the `/schedule_a/` endpoints.    <b class=\"body\" id=\"getting_started_head\">Getting started with the openFEC API</b><br>    If you would like to use the FEC's API programmatically, you can sign up for your own API key using our form. Alternatively, you can still try out our API without an API key by using the web interface and using DEMO_KEY. Note that when you use the openFEC API you are subject to the [Terms of Service](https://github.com/fecgov/FEC/blob/master/TERMS-OF-SERVICE.md) and [Acceptable Use policy](https://github.com/fecgov/FEC/blob/master/ACCEPTABLE-USE-POLICY.md).    Signing up for an API key will enable you to place up to 1,000 calls an hour. Each call is limited to 100 results per page. You can email questions, comments or a request to get a key for 7,200 calls an hour (120 calls per minute) to <a href=\"mailto:APIinfo@fec.gov\">APIinfo@fec.gov</a>. You can also ask questions and discuss the data in a community led [group](https://groups.google.com/forum/#!forum/fec-data).    The model definitions and schema are available at [/swagger](/swagger/). This is useful for making wrappers and exploring the data.    A few restrictions limit the way you can use FEC data. For example, you can’t use contributor lists for commercial purposes or to solicit donations. [Learn more here](https://www.fec.gov/updates/sale-or-use-contributor-information/).    [Inspect our source code](https://github.com/fecgov/openFEC). We welcome issues and pull requests!    <p><br></p> <h2 class=\"title\" id=\"signup_head\">Sign up for an API key</h2> <div id=\"apidatagov_signup\">Loading signup form...</div>
#
# The version of the OpenAPI document: 1.0
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Loans operations
#' @description openapi.Loans
#' @format An \code{R6Class} generator object
#' @field apiClient Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ SchedulesScheduleCGet } \emph{  }
#'  Schedule C shows all loans, endorsements and loan guarantees a committee receives or makes.  The committee continues to report the loan until it is repaid. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } max.image.number character
#' \item \emph{ @param } max.amount character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } min.payment.to.date integer
#' \item \emph{ @param } max.payment.to.date integer
#' \item \emph{ @param } line.number character
#' \item \emph{ @param } committee.id list( character )
#' \item \emph{ @param } sort character
#' \item \emph{ @param } candidate.name list( character )
#' \item \emph{ @param } min.amount character
#' \item \emph{ @param } last.index integer
#' \item \emph{ @param } max.incurred.date character
#' \item \emph{ @param } min.image.number character
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } image.number list( character )
#' \item \emph{ @param } min.incurred.date character
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } loan.source.name list( character )
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{InlineResponseDefault3} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : InlineResponseDefault3 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SchedulesScheduleCSubIdGet } \emph{  }
#'  Schedule C shows all loans, endorsements and loan guarantees a committee receives or makes.  The committee continues to report the loan until it is repaid. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } sub.id character
#' \item \emph{ @param } sort character
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } page integer
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @returnType } \link{InlineResponseDefault3} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : InlineResponseDefault3 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  SchedulesScheduleCGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.max.image.number <- 'max.image.number_example' # character | 
#' var.max.amount <- 'max.amount_example' # character |  Filter for all amounts less than a value. 
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.min.payment.to.date <- 56 # integer |  Minimum payment to date 
#' var.max.payment.to.date <- 56 # integer |  Maximum payment to date 
#' var.line.number <- 'line.number_example' # character |  Filter for form and line number using the following format: `FORM-LINENUMBER`.  For example an argument such as `F3X-16` would filter down to all entries from form `F3X` line number `16`. 
#' var.committee.id <- ['committee.id_example'] # array[character] |  A unique identifier assigned to each committee or filer registered with the FEC. In general committee id's begin with the letter C which is followed by eight digits. 
#' var.sort <- '-incurred_date' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.candidate.name <- ['candidate.name_example'] # array[character] | Name of candidate running for office
#' var.min.amount <- 'min.amount_example' # character |  Filter for all amounts greater than a value. 
#' var.last.index <- 56 # integer | Index of last result from previous page
#' var.max.incurred.date <- 'max.incurred.date_example' # character |  Maximum incurred date 
#' var.min.image.number <- 'min.image.number_example' # character | 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.image.number <- ['image.number_example'] # array[character] |  An unique identifier for each page where the electronic or paper filing is reported. 
#' var.min.incurred.date <- 'min.incurred.date_example' # character |  Minimum incurred date 
#' var.sort.nulls.last <- TRUE # character | Toggle that sorts null values last
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.loan.source.name <- ['loan.source.name_example'] # array[character] | Source of the loan (i.e., bank loan, brokerage account, credit card, home equity line of credit,               other line of credit, or personal funds of the candidate
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- LoansApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$SchedulesScheduleCGet(var.api.key, max.image.number=var.max.image.number, max.amount=var.max.amount, per.page=var.per.page, min.payment.to.date=var.min.payment.to.date, max.payment.to.date=var.max.payment.to.date, line.number=var.line.number, committee.id=var.committee.id, sort=var.sort, candidate.name=var.candidate.name, min.amount=var.min.amount, last.index=var.last.index, max.incurred.date=var.max.incurred.date, min.image.number=var.min.image.number, sort.hide.null=var.sort.hide.null, image.number=var.image.number, min.incurred.date=var.min.incurred.date, sort.nulls.last=var.sort.nulls.last, sort.null.only=var.sort.null.only, loan.source.name=var.loan.source.name, page=var.page)
#'
#'
#' ####################  SchedulesScheduleCSubIdGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.sub.id <- 'sub.id_example' # character | 
#' var.sort <- 'null' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#'
#' api.instance <- LoansApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$SchedulesScheduleCSubIdGet(var.api.key, var.sub.id, sort=var.sort, sort.hide.null=var.sort.hide.null, per.page=var.per.page, sort.nulls.last=var.sort.nulls.last, page=var.page, sort.null.only=var.sort.null.only)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
LoansApi <- R6::R6Class(
  'LoansApi',
  public = list(
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    SchedulesScheduleCGet = function(api.key, max.image.number=NULL, max.amount=NULL, per.page=20, min.payment.to.date=NULL, max.payment.to.date=NULL, line.number=NULL, committee.id=NULL, sort='-incurred_date', candidate.name=NULL, min.amount=NULL, last.index=NULL, max.incurred.date=NULL, min.image.number=NULL, sort.hide.null=FALSE, image.number=NULL, min.incurred.date=NULL, sort.nulls.last=TRUE, sort.null.only=FALSE, loan.source.name=NULL, page=1, ...){
      apiResponse <- self$SchedulesScheduleCGetWithHttpInfo(api.key, max.image.number, max.amount, per.page, min.payment.to.date, max.payment.to.date, line.number, committee.id, sort, candidate.name, min.amount, last.index, max.incurred.date, min.image.number, sort.hide.null, image.number, min.incurred.date, sort.nulls.last, sort.null.only, loan.source.name, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    SchedulesScheduleCGetWithHttpInfo = function(api.key, max.image.number=NULL, max.amount=NULL, per.page=20, min.payment.to.date=NULL, max.payment.to.date=NULL, line.number=NULL, committee.id=NULL, sort='-incurred_date', candidate.name=NULL, min.amount=NULL, last.index=NULL, max.incurred.date=NULL, min.image.number=NULL, sort.hide.null=FALSE, image.number=NULL, min.incurred.date=NULL, sort.nulls.last=TRUE, sort.null.only=FALSE, loan.source.name=NULL, page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['max_image_number'] <- max.image.number

      queryParams['max_amount'] <- max.amount

      queryParams['per_page'] <- per.page

      queryParams['min_payment_to_date'] <- min.payment.to.date

      queryParams['max_payment_to_date'] <- max.payment.to.date

      queryParams['line_number'] <- line.number

      queryParams['committee_id'] <- committee.id

      queryParams['sort'] <- sort

      queryParams['candidate_name'] <- candidate.name

      queryParams['min_amount'] <- min.amount

      queryParams['last_index'] <- last.index

      queryParams['max_incurred_date'] <- max.incurred.date

      queryParams['min_image_number'] <- min.image.number

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['image_number'] <- image.number

      queryParams['min_incurred_date'] <- min.incurred.date

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['loan_source_name'] <- loan.source.name

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/schedules/schedule_c/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "InlineResponseDefault3", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    SchedulesScheduleCSubIdGet = function(api.key, sub.id, sort='null', sort.hide.null=FALSE, per.page=20, sort.nulls.last=FALSE, page=1, sort.null.only=FALSE, ...){
      apiResponse <- self$SchedulesScheduleCSubIdGetWithHttpInfo(api.key, sub.id, sort, sort.hide.null, per.page, sort.nulls.last, page, sort.null.only, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    SchedulesScheduleCSubIdGetWithHttpInfo = function(api.key, sub.id, sort='null', sort.hide.null=FALSE, per.page=20, sort.nulls.last=FALSE, page=1, sort.null.only=FALSE, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      if (missing(`sub.id`)) {
        stop("Missing required parameter `sub.id`.")
      }

      queryParams['sort'] <- sort

      queryParams['api_key'] <- api.key

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['per_page'] <- per.page

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['page'] <- page

      queryParams['sort_null_only'] <- sort.null.only

      body <- NULL
      urlPath <- "/schedules/schedule_c/{sub_id}/"
      if (!missing(`sub.id`)) {
        urlPath <- gsub(paste0("\\{", "sub_id", "\\}"), URLencode(as.character(`sub.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "InlineResponseDefault3", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    }
  )
)
