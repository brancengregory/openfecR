# OpenFEC
#
# This application programming interface (API) allows you to explore the way candidates and committees fund their campaigns.    The Federal Election Commission (FEC) API is a RESTful web service supporting full-text and field-specific searches on FEC data. [Bulk downloads](https://www.fec.gov/data/advanced/?tab=bulk-data) are available on the current site. Information is tied to the underlying forms by file ID and image ID. Data is updated nightly.    There are a lot of data, and a good place to start is to use search to find interesting candidates and committees. Then, you can use their IDs to find report or line item details with the other endpoints. If you are interested in individual donors, check out contributor information in the `/schedule_a/` endpoints.    <b class=\"body\" id=\"getting_started_head\">Getting started with the openFEC API</b><br>    If you would like to use the FEC's API programmatically, you can sign up for your own API key using our form. Alternatively, you can still try out our API without an API key by using the web interface and using DEMO_KEY. Note that when you use the openFEC API you are subject to the [Terms of Service](https://github.com/fecgov/FEC/blob/master/TERMS-OF-SERVICE.md) and [Acceptable Use policy](https://github.com/fecgov/FEC/blob/master/ACCEPTABLE-USE-POLICY.md).    Signing up for an API key will enable you to place up to 1,000 calls an hour. Each call is limited to 100 results per page. You can email questions, comments or a request to get a key for 7,200 calls an hour (120 calls per minute) to <a href=\"mailto:APIinfo@fec.gov\">APIinfo@fec.gov</a>. You can also ask questions and discuss the data in a community led [group](https://groups.google.com/forum/#!forum/fec-data).    The model definitions and schema are available at [/swagger](/swagger/). This is useful for making wrappers and exploring the data.    A few restrictions limit the way you can use FEC data. For example, you can’t use contributor lists for commercial purposes or to solicit donations. [Learn more here](https://www.fec.gov/updates/sale-or-use-contributor-information/).    [Inspect our source code](https://github.com/fecgov/openFEC). We welcome issues and pull requests!    <p><br></p> <h2 class=\"title\" id=\"signup_head\">Sign up for an API key</h2> <div id=\"apidatagov_signup\">Loading signup form...</div>
#
# The version of the OpenAPI document: 1.0
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FilerResources operations
#' @description openapi.FilerResources
#' @format An \code{R6Class} generator object
#' @field apiClient Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ RadAnalystGet } \emph{  }
#'  Use this endpoint to look up the RAD Analyst for a committee.  The mission of the Reports Analysis Division (RAD) is to ensure that campaigns and political committees file timely and accurate reports that fully disclose their financial activities.  RAD is responsible for reviewing statements and financial reports filed by political committees participating in federal elections, providing assistance and guidance to the committees to properly file their reports, and for taking appropriate action to ensure compliance with the Federal Election Campaign Act (FECA). 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } telephone.ext list( integer )
#' \item \emph{ @param } analyst.id list( integer )
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } min.assignment.update.date character
#' \item \emph{ @param } email list( character )
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } name list( character )
#' \item \emph{ @param } max.assignment.update.date character
#' \item \emph{ @param } analyst.short.id list( integer )
#' \item \emph{ @param } committee.id list( character )
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } sort character
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } title list( character )
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{RadAnalystPage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : RadAnalystPage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ StateElectionOfficeGet } \emph{  }
#'  State laws and procedures govern elections for state or local offices as well as how candidates appear on election ballots. Contact the appropriate state election office for more information. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } state character
#' \item \emph{ @param } sort character
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } page integer
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @returnType } \link{StateElectionOfficeInfoPage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : StateElectionOfficeInfoPage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  RadAnalystGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.telephone.ext <- [56] # array[integer] | Telephone extension of RAD analyst
#' var.analyst.id <- [56] # array[integer] | ID of RAD analyst
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.min.assignment.update.date <- 'min.assignment.update.date_example' # character | Filter results for assignment updates made after this date
#' var.email <- ['email_example'] # array[character] | Email of RAD analyst
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.name <- ['name_example'] # array[character] | Name of RAD analyst
#' var.max.assignment.update.date <- 'max.assignment.update.date_example' # character | Filter results for assignment updates made before this date
#' var.analyst.short.id <- [56] # array[integer] | Short ID of RAD analyst
#' var.committee.id <- ['committee.id_example'] # array[character] |  A unique identifier assigned to each committee or filer registered with the FEC. In general committee id's begin with the letter C which is followed by eight digits. 
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.sort <- 'null' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.title <- ['title_example'] # array[character] | Title of RAD analyst
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- FilerResourcesApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$RadAnalystGet(var.api.key, telephone.ext=var.telephone.ext, analyst.id=var.analyst.id, sort.hide.null=var.sort.hide.null, min.assignment.update.date=var.min.assignment.update.date, email=var.email, per.page=var.per.page, name=var.name, max.assignment.update.date=var.max.assignment.update.date, analyst.short.id=var.analyst.short.id, committee.id=var.committee.id, sort.null.only=var.sort.null.only, sort=var.sort, sort.nulls.last=var.sort.nulls.last, title=var.title, page=var.page)
#'
#'
#' ####################  StateElectionOfficeGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.state <- 'state_example' # character |  Enter a state (Ex: AK, TX, VA etc..) to find the local election offices contact information.  
#' var.sort <- 'null' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#'
#' api.instance <- FilerResourcesApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$StateElectionOfficeGet(var.api.key, var.state, sort=var.sort, sort.hide.null=var.sort.hide.null, per.page=var.per.page, sort.nulls.last=var.sort.nulls.last, page=var.page, sort.null.only=var.sort.null.only)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
FilerResourcesApi <- R6::R6Class(
  'FilerResourcesApi',
  public = list(
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    RadAnalystGet = function(api.key, telephone.ext=NULL, analyst.id=NULL, sort.hide.null=FALSE, min.assignment.update.date=NULL, email=NULL, per.page=20, name=NULL, max.assignment.update.date=NULL, analyst.short.id=NULL, committee.id=NULL, sort.null.only=FALSE, sort='null', sort.nulls.last=FALSE, title=NULL, page=1, ...){
      apiResponse <- self$RadAnalystGetWithHttpInfo(api.key, telephone.ext, analyst.id, sort.hide.null, min.assignment.update.date, email, per.page, name, max.assignment.update.date, analyst.short.id, committee.id, sort.null.only, sort, sort.nulls.last, title, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    RadAnalystGetWithHttpInfo = function(api.key, telephone.ext=NULL, analyst.id=NULL, sort.hide.null=FALSE, min.assignment.update.date=NULL, email=NULL, per.page=20, name=NULL, max.assignment.update.date=NULL, analyst.short.id=NULL, committee.id=NULL, sort.null.only=FALSE, sort='null', sort.nulls.last=FALSE, title=NULL, page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['telephone_ext'] <- telephone.ext

      queryParams['analyst_id'] <- analyst.id

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['min_assignment_update_date'] <- min.assignment.update.date

      queryParams['email'] <- email

      queryParams['per_page'] <- per.page

      queryParams['name'] <- name

      queryParams['max_assignment_update_date'] <- max.assignment.update.date

      queryParams['analyst_short_id'] <- analyst.short.id

      queryParams['committee_id'] <- committee.id

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['sort'] <- sort

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['title'] <- title

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/rad-analyst/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "RadAnalystPage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    StateElectionOfficeGet = function(api.key, state, sort='null', sort.hide.null=FALSE, per.page=20, sort.nulls.last=FALSE, page=1, sort.null.only=FALSE, ...){
      apiResponse <- self$StateElectionOfficeGetWithHttpInfo(api.key, state, sort, sort.hide.null, per.page, sort.nulls.last, page, sort.null.only, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    StateElectionOfficeGetWithHttpInfo = function(api.key, state, sort='null', sort.hide.null=FALSE, per.page=20, sort.nulls.last=FALSE, page=1, sort.null.only=FALSE, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      if (missing(`state`)) {
        stop("Missing required parameter `state`.")
      }

      queryParams['sort'] <- sort

      queryParams['api_key'] <- api.key

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['state'] <- state

      queryParams['per_page'] <- per.page

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['page'] <- page

      queryParams['sort_null_only'] <- sort.null.only

      body <- NULL
      urlPath <- "/state-election-office/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "StateElectionOfficeInfoPage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    }
  )
)
