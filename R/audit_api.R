# OpenFEC
#
# This application programming interface (API) allows you to explore the way candidates and committees fund their campaigns.    The Federal Election Commission (FEC) API is a RESTful web service supporting full-text and field-specific searches on FEC data. [Bulk downloads](https://www.fec.gov/data/advanced/?tab=bulk-data) are available on the current site. Information is tied to the underlying forms by file ID and image ID. Data is updated nightly.    There are a lot of data, and a good place to start is to use search to find interesting candidates and committees. Then, you can use their IDs to find report or line item details with the other endpoints. If you are interested in individual donors, check out contributor information in the `/schedule_a/` endpoints.    <b class=\"body\" id=\"getting_started_head\">Getting started with the openFEC API</b><br>    If you would like to use the FEC's API programmatically, you can sign up for your own API key using our form. Alternatively, you can still try out our API without an API key by using the web interface and using DEMO_KEY. Note that when you use the openFEC API you are subject to the [Terms of Service](https://github.com/fecgov/FEC/blob/master/TERMS-OF-SERVICE.md) and [Acceptable Use policy](https://github.com/fecgov/FEC/blob/master/ACCEPTABLE-USE-POLICY.md).    Signing up for an API key will enable you to place up to 1,000 calls an hour. Each call is limited to 100 results per page. You can email questions, comments or a request to get a key for 7,200 calls an hour (120 calls per minute) to <a href=\"mailto:APIinfo@fec.gov\">APIinfo@fec.gov</a>. You can also ask questions and discuss the data in a community led [group](https://groups.google.com/forum/#!forum/fec-data).    The model definitions and schema are available at [/swagger](/swagger/). This is useful for making wrappers and exploring the data.    A few restrictions limit the way you can use FEC data. For example, you can’t use contributor lists for commercial purposes or to solicit donations. [Learn more here](https://www.fec.gov/updates/sale-or-use-contributor-information/).    [Inspect our source code](https://github.com/fecgov/openFEC). We welcome issues and pull requests!    <p><br></p> <h2 class=\"title\" id=\"signup_head\">Sign up for an API key</h2> <div id=\"apidatagov_signup\">Loading signup form...</div>
#
# The version of the OpenAPI document: 1.0
#
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Audit operations
#' @description openapi.Audit
#' @format An \code{R6Class} generator object
#' @field apiClient Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ AuditCaseGet } \emph{  }
#'  This endpoint contains Final Audit Reports approved by the Commission since inception. The search can be based on information about the audited committee (Name, FEC ID Number, Type,  Election Cycle) or the issues covered in the report.
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } primary.category.id character
#' \item \emph{ @param } max.election.cycle integer
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } committee.id list( character )
#' \item \emph{ @param } committee.type list( character )
#' \item \emph{ @param } sort list( character )
#' \item \emph{ @param } audit.case.id list( character )
#' \item \emph{ @param } q list( character )
#' \item \emph{ @param } cycle list( integer )
#' \item \emph{ @param } committee.designation character
#' \item \emph{ @param } min.election.cycle integer
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } qq list( character )
#' \item \emph{ @param } candidate.id list( character )
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } sub.category.id character
#' \item \emph{ @param } page integer
#' \item \emph{ @param } audit.id list( integer )
#' \item \emph{ @returnType } \link{AuditCasePage} \cr
#'
#'
#' \item status code : 0 |
#'
#' \item return type : AuditCasePage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ AuditCategoryGet } \emph{  }
#'  This lists the options for the categories and subcategories available in the /audit-search/ endpoint.
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } primary.category.id list( character )
#' \item \emph{ @param } primary.category.name list( character )
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } sort character
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{AuditCategoryPage} \cr
#'
#'
#' \item status code : 0 |
#'
#' \item return type : AuditCategoryPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ AuditPrimaryCategoryGet } \emph{  }
#'  This lists the options for the primary categories available in the /audit-search/ endpoint.
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } primary.category.id list( character )
#' \item \emph{ @param } primary.category.name list( character )
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } sort character
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{AuditPrimaryCategoryPage} \cr
#'
#'
#' \item status code : 0 |
#'
#' \item return type : AuditPrimaryCategoryPage
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ NamesAuditCandidatesGet } \emph{  }
#'  Search for candidates or committees by name. If you&#39;re looking for information on a particular person or group, using a name to find the &#x60;candidate_id&#x60; or &#x60;committee_id&#x60; on this endpoint can be a helpful first step.
#'
#' \itemize{
#' \item \emph{ @param } q list( character )
#' \item \emph{ @param } api.key character
#' \item \emph{ @returnType } \link{AuditCandidateSearchList} \cr
#'
#'
#' \item status code : 0 |
#'
#' \item return type : AuditCandidateSearchList
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ NamesAuditCommitteesGet } \emph{  }
#'  Search for candidates or committees by name. If you&#39;re looking for information on a particular person or group, using a name to find the &#x60;candidate_id&#x60; or &#x60;committee_id&#x60; on this endpoint can be a helpful first step.
#'
#' \itemize{
#' \item \emph{ @param } q list( character )
#' \item \emph{ @param } api.key character
#' \item \emph{ @returnType } \link{AuditCommitteeSearchList} \cr
#'
#'
#' \item status code : 0 |
#'
#' \item return type : AuditCommitteeSearchList
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  AuditCaseGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup.
#' var.primary.category.id <- 'all' # character |  Audit category ID (table PK)
#' var.max.election.cycle <- 56 # integer |  Filter records to only those that are applicable to a given two-year period. This cycle follows the traditional House election cycle and subdivides the presidential and Senate elections into comparable two-year blocks. The cycle begins with an odd year and is named for its ending, even year.
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.committee.id <- ['committee.id_example'] # array[character] |  A unique identifier assigned to each committee or filer registered with the FEC. In general committee id's begin with the letter C which is followed by eight digits.
#' var.committee.type <- ['committee.type_example'] # array[character] | The one-letter type code of the organization:         - C communication cost         - D delegate         - E electioneering communication         - H House         - I independent expenditor (person or group)         - N PAC - nonqualified         - O independent expenditure-only (super PACs)         - P presidential         - Q PAC - qualified         - S Senate         - U single candidate independent expenditure         - V PAC with non-contribution account, nonqualified         - W PAC with non-contribution account, qualified         - X party, nonqualified         - Y party, qualified         - Z national party non-federal account
#' var.sort <- ["-cycle","committee_name"] # array[character] | Provide a field to sort by. Use - for descending order.
#' var.audit.case.id <- ['audit.case.id_example'] # array[character] |  Primary/foreign key for audit tables
#' var.q <- ['q_example'] # array[character] | The name of the committee. If a committee changes its name,     the most recent name will be shown. Committee names are not unique. Use committee_id     for looking up records.
#' var.cycle <- [56] # array[integer] |  Filter records to only those that are applicable to a given two-year period. This cycle follows the traditional House election cycle and subdivides the presidential and Senate elections into comparable two-year blocks. The cycle begins with an odd year and is named for its ending, even year.
#' var.committee.designation <- 'committee.designation_example' # character | Type of committee:         - H or S - Congressional         - P - Presidential         - X or Y or Z - Party         - N or Q - PAC         - I - Independent expenditure         - O - Super PAC
#' var.min.election.cycle <- 56 # integer |  Filter records to only those that are applicable to a given two-year period. This cycle follows the traditional House election cycle and subdivides the presidential and Senate elections into comparable two-year blocks. The cycle begins with an odd year and is named for its ending, even year.
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.qq <- ['qq_example'] # array[character] | Name of candidate running for office
#' var.candidate.id <- ['candidate.id_example'] # array[character] |  A unique identifier assigned to each candidate registered with the FEC. If a person runs for several offices, that person will have separate candidate IDs for each office.
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.sub.category.id <- 'all' # character |  The finding id of an audit. Finding are a category of broader issues. Each category has an unique ID.
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#' var.audit.id <- [56] # array[integer] |  The audit issue. Each subcategory has an unique ID
#'
#' api.instance <- AuditApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$AuditCaseGet(var.api.key, primary.category.id=var.primary.category.id, max.election.cycle=var.max.election.cycle, per.page=var.per.page, committee.id=var.committee.id, committee.type=var.committee.type, sort=var.sort, audit.case.id=var.audit.case.id, q=var.q, cycle=var.cycle, committee.designation=var.committee.designation, min.election.cycle=var.min.election.cycle, sort.hide.null=var.sort.hide.null, sort.nulls.last=var.sort.nulls.last, qq=var.qq, candidate.id=var.candidate.id, sort.null.only=var.sort.null.only, sub.category.id=var.sub.category.id, page=var.page, audit.id=var.audit.id)
#'
#'
#' ####################  AuditCategoryGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup.
#' var.primary.category.id <- ['primary.category.id_example'] # array[character] |  Audit category ID (table PK)
#' var.primary.category.name <- ['primary.category.name_example'] # array[character] | Primary Audit Category     - No Findings or Issues/Not a Committee     - Net Outstanding Campaign/Convention Expenditures/Obligations     - Payments/Disgorgements     - Allocation Issues     - Prohibited Contributions     - Disclosure     - Recordkeeping     - Repayment to US Treasury     - Other     - Misstatement of Financial Activity     - Excessive Contributions     - Failure to File Reports/Schedules/Notices     - Loans     - Referred Findings Not Listed
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.sort <- 'primary_category_name' # character | Provide a field to sort by. Use `-` for descending order.
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- AuditApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$AuditCategoryGet(var.api.key, primary.category.id=var.primary.category.id, primary.category.name=var.primary.category.name, sort.hide.null=var.sort.hide.null, per.page=var.per.page, sort.nulls.last=var.sort.nulls.last, sort.null.only=var.sort.null.only, sort=var.sort, page=var.page)
#'
#'
#' ####################  AuditPrimaryCategoryGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup.
#' var.primary.category.id <- ['primary.category.id_example'] # array[character] |  Audit category ID (table PK)
#' var.primary.category.name <- ['primary.category.name_example'] # array[character] | Primary Audit Category     - No Findings or Issues/Not a Committee     - Net Outstanding Campaign/Convention Expenditures/Obligations     - Payments/Disgorgements     - Allocation Issues     - Prohibited Contributions     - Disclosure     - Recordkeeping     - Repayment to US Treasury     - Other     - Misstatement of Financial Activity     - Excessive Contributions     - Failure to File Reports/Schedules/Notices     - Loans     - Referred Findings Not Listed
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.sort <- 'primary_category_name' # character | Provide a field to sort by. Use `-` for descending order.
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- AuditApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$AuditPrimaryCategoryGet(var.api.key, primary.category.id=var.primary.category.id, primary.category.name=var.primary.category.name, sort.hide.null=var.sort.hide.null, per.page=var.per.page, sort.nulls.last=var.sort.nulls.last, sort.null.only=var.sort.null.only, sort=var.sort, page=var.page)
#'
#'
#' ####################  NamesAuditCandidatesGet  ####################
#'
#' library(openapi)
#' var.q <- ['q_example'] # array[character] | Name (candidate or committee) to search for
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup.
#'
#' api.instance <- AuditApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$NamesAuditCandidatesGet(var.q, var.api.key)
#'
#'
#' ####################  NamesAuditCommitteesGet  ####################
#'
#' library(openapi)
#' var.q <- ['q_example'] # array[character] | Name (candidate or committee) to search for
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup.
#'
#' api.instance <- AuditApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$NamesAuditCommitteesGet(var.q, var.api.key)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
AuditApi <- R6::R6Class(
  'AuditApi',
  public = list(
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    AuditCaseGet = function(api.key, primary.category.id='all', max.election.cycle=NULL, per.page=20, committee.id=NULL, committee.type=NULL, sort=c("-cycle","committee_name"), audit.case.id=NULL, q=NULL, cycle=NULL, committee.designation=NULL, min.election.cycle=NULL, sort.hide.null=FALSE, sort.nulls.last=FALSE, qq=NULL, candidate.id=NULL, sort.null.only=FALSE, sub.category.id='all', page=1, audit.id=NULL, ...){
      apiResponse <- self$AuditCaseGetWithHttpInfo(api.key, primary.category.id, max.election.cycle, per.page, committee.id, committee.type, sort, audit.case.id, q, cycle, committee.designation, min.election.cycle, sort.hide.null, sort.nulls.last, qq, candidate.id, sort.null.only, sub.category.id, page, audit.id, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    AuditCaseGetWithHttpInfo = function(api.key, primary.category.id='all', max.election.cycle=NULL, per.page=20, committee.id=NULL, committee.type=NULL, sort=c("-cycle","committee_name"), audit.case.id=NULL, q=NULL, cycle=NULL, committee.designation=NULL, min.election.cycle=NULL, sort.hide.null=FALSE, sort.nulls.last=FALSE, qq=NULL, candidate.id=NULL, sort.null.only=FALSE, sub.category.id='all', page=1, audit.id=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['primary_category_id'] <- primary.category.id

      queryParams['max_election_cycle'] <- max.election.cycle

      queryParams['per_page'] <- per.page

      queryParams['committee_id'] <- committee.id

      queryParams['committee_type'] <- committee.type

      queryParams['sort'] <- sort

      queryParams['audit_case_id'] <- audit.case.id

      queryParams['q'] <- q

      queryParams['cycle'] <- cycle

      queryParams['committee_designation'] <- committee.designation

      queryParams['min_election_cycle'] <- min.election.cycle

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['qq'] <- qq

      queryParams['candidate_id'] <- candidate.id

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['sub_category_id'] <- sub.category.id

      queryParams['page'] <- page

      queryParams['audit_id'] <- audit.id

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/audit-case/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "AuditCasePage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    AuditCategoryGet = function(api.key, primary.category.id=NULL, primary.category.name=NULL, sort.hide.null=FALSE, per.page=20, sort.nulls.last=FALSE, sort.null.only=FALSE, sort='primary_category_name', page=1, ...){
      apiResponse <- self$AuditCategoryGetWithHttpInfo(api.key, primary.category.id, primary.category.name, sort.hide.null, per.page, sort.nulls.last, sort.null.only, sort, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    AuditCategoryGetWithHttpInfo = function(api.key, primary.category.id=NULL, primary.category.name=NULL, sort.hide.null=FALSE, per.page=20, sort.nulls.last=FALSE, sort.null.only=FALSE, sort='primary_category_name', page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['primary_category_id'] <- primary.category.id

      queryParams['primary_category_name'] <- primary.category.name

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['per_page'] <- per.page

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['sort'] <- sort

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/audit-category/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "AuditCategoryPage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    AuditPrimaryCategoryGet = function(api.key, primary.category.id=NULL, primary.category.name=NULL, sort.hide.null=FALSE, per.page=20, sort.nulls.last=FALSE, sort.null.only=FALSE, sort='primary_category_name', page=1, ...){
      apiResponse <- self$AuditPrimaryCategoryGetWithHttpInfo(api.key, primary.category.id, primary.category.name, sort.hide.null, per.page, sort.nulls.last, sort.null.only, sort, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    AuditPrimaryCategoryGetWithHttpInfo = function(api.key, primary.category.id=NULL, primary.category.name=NULL, sort.hide.null=FALSE, per.page=20, sort.nulls.last=FALSE, sort.null.only=FALSE, sort='primary_category_name', page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['primary_category_id'] <- primary.category.id

      queryParams['primary_category_name'] <- primary.category.name

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['per_page'] <- per.page

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['sort'] <- sort

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/audit-primary-category/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "AuditPrimaryCategoryPage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    NamesAuditCandidatesGet = function(q, api.key, ...){
      apiResponse <- self$NamesAuditCandidatesGetWithHttpInfo(q, api.key, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    NamesAuditCandidatesGetWithHttpInfo = function(q, api.key, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`q`)) {
        stop("Missing required parameter `q`.")
      }

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['q'] <- q

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/names/audit_candidates/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "AuditCandidateSearchList", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    NamesAuditCommitteesGet = function(q, api.key, ...){
      apiResponse <- self$NamesAuditCommitteesGetWithHttpInfo(q, api.key, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    NamesAuditCommitteesGetWithHttpInfo = function(q, api.key, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`q`)) {
        stop("Missing required parameter `q`.")
      }

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['q'] <- q

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/names/audit_committees/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "AuditCommitteeSearchList", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    }
  )
)
