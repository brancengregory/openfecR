# OpenFEC
#
# This application programming interface (API) allows you to explore the way candidates and committees fund their campaigns.    The Federal Election Commission (FEC) API is a RESTful web service supporting full-text and field-specific searches on FEC data. [Bulk downloads](https://www.fec.gov/data/advanced/?tab=bulk-data) are available on the current site. Information is tied to the underlying forms by file ID and image ID. Data is updated nightly.    There are a lot of data, and a good place to start is to use search to find interesting candidates and committees. Then, you can use their IDs to find report or line item details with the other endpoints. If you are interested in individual donors, check out contributor information in the `/schedule_a/` endpoints.    <b class=\"body\" id=\"getting_started_head\">Getting started with the openFEC API</b><br>    If you would like to use the FEC's API programmatically, you can sign up for your own API key using our form. Alternatively, you can still try out our API without an API key by using the web interface and using DEMO_KEY. Note that when you use the openFEC API you are subject to the [Terms of Service](https://github.com/fecgov/FEC/blob/master/TERMS-OF-SERVICE.md) and [Acceptable Use policy](https://github.com/fecgov/FEC/blob/master/ACCEPTABLE-USE-POLICY.md).    Signing up for an API key will enable you to place up to 1,000 calls an hour. Each call is limited to 100 results per page. You can email questions, comments or a request to get a key for 7,200 calls an hour (120 calls per minute) to <a href=\"mailto:APIinfo@fec.gov\">APIinfo@fec.gov</a>. You can also ask questions and discuss the data in a community led [group](https://groups.google.com/forum/#!forum/fec-data).    The model definitions and schema are available at [/swagger](/swagger/). This is useful for making wrappers and exploring the data.    A few restrictions limit the way you can use FEC data. For example, you can’t use contributor lists for commercial purposes or to solicit donations. [Learn more here](https://www.fec.gov/updates/sale-or-use-contributor-information/).    [Inspect our source code](https://github.com/fecgov/openFEC). We welcome issues and pull requests!    <p><br></p> <h2 class=\"title\" id=\"signup_head\">Sign up for an API key</h2> <div id=\"apidatagov_signup\">Loading signup form...</div>
#
# The version of the OpenAPI document: 1.0
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Committee operations
#' @description openapi.Committee
#' @format An \code{R6Class} generator object
#' @field apiClient Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ CandidateCandidateIdCommitteesGet } \emph{  }
#'  This endpoint is useful for finding detailed information about a particular committee or filer. Use the &#x60;committee_id&#x60; to find the most recent information about the committee. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } candidate.id character
#' \item \emph{ @param } designation Enum < [, A, J, P, U, B, D] > 
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } year list( integer )
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } organization.type Enum < [, C, L, M, T, V, W] > 
#' \item \emph{ @param } committee.type Enum < [, C, D, E, H, I, N, O, P, Q, S, U, V, W, X, Y, Z] > 
#' \item \emph{ @param } sort character
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } filing.frequency Enum < [, A, M, N, Q, T, W, -A, -T] > 
#' \item \emph{ @param } cycle list( integer )
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{CommitteeDetailPage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : CommitteeDetailPage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CandidateCandidateIdCommitteesHistoryCycleGet } \emph{  }
#'  Explore a filer&#39;s characteristics over time. This can be particularly useful if the committees change treasurers, designation, or &#x60;committee_type&#x60;. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } cycle integer
#' \item \emph{ @param } candidate.id character
#' \item \emph{ @param } designation Enum < [, A, J, P, U, B, D] > 
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } election.full character
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } sort character
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{CommitteeHistoryPage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : CommitteeHistoryPage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CandidateCandidateIdCommitteesHistoryGet } \emph{  }
#'  Explore a filer&#39;s characteristics over time. This can be particularly useful if the committees change treasurers, designation, or &#x60;committee_type&#x60;. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } candidate.id character
#' \item \emph{ @param } designation Enum < [, A, J, P, U, B, D] > 
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } election.full character
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } sort character
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{CommitteeHistoryPage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : CommitteeHistoryPage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CommitteeCommitteeIdGet } \emph{  }
#'  This endpoint is useful for finding detailed information about a particular committee or filer. Use the &#x60;committee_id&#x60; to find the most recent information about the committee. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } committee.id character
#' \item \emph{ @param } designation Enum < [, A, J, P, U, B, D] > 
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } year list( integer )
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } organization.type Enum < [, C, L, M, T, V, W] > 
#' \item \emph{ @param } committee.type Enum < [, C, D, E, H, I, N, O, P, Q, S, U, V, W, X, Y, Z] > 
#' \item \emph{ @param } sort character
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } filing.frequency Enum < [, A, M, N, Q, T, W, -A, -T] > 
#' \item \emph{ @param } cycle list( integer )
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{CommitteeDetailPage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : CommitteeDetailPage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CommitteeCommitteeIdHistoryCycleGet } \emph{  }
#'  Explore a filer&#39;s characteristics over time. This can be particularly useful if the committees change treasurers, designation, or &#x60;committee_type&#x60;. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } cycle integer
#' \item \emph{ @param } committee.id character
#' \item \emph{ @param } designation Enum < [, A, J, P, U, B, D] > 
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } election.full character
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } sort character
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{CommitteeHistoryPage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : CommitteeHistoryPage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CommitteeCommitteeIdHistoryGet } \emph{  }
#'  Explore a filer&#39;s characteristics over time. This can be particularly useful if the committees change treasurers, designation, or &#x60;committee_type&#x60;. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } committee.id character
#' \item \emph{ @param } designation Enum < [, A, J, P, U, B, D] > 
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } election.full character
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } sort character
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{CommitteeHistoryPage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : CommitteeHistoryPage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ CommitteesGet } \emph{  }
#'  Fetch basic information about committees and filers. Use parameters to filter for particular characteristics.  
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } designation Enum < [, A, J, P, U, B, D] > 
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } committee.id list( character )
#' \item \emph{ @param } organization.type Enum < [, C, L, M, T, V, W] > 
#' \item \emph{ @param } committee.type Enum < [, C, D, E, H, I, N, O, P, Q, S, U, V, W, X, Y, Z] > 
#' \item \emph{ @param } sort character
#' \item \emph{ @param } max.last.f1.date character
#' \item \emph{ @param } q list( character )
#' \item \emph{ @param } filing.frequency Enum < [, A, M, N, Q, T, W, -A, -T] > 
#' \item \emph{ @param } state list( character )
#' \item \emph{ @param } cycle list( integer )
#' \item \emph{ @param } min.first.file.date character
#' \item \emph{ @param } sponsor.candidate.id list( character )
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } year list( integer )
#' \item \emph{ @param } max.first.file.date character
#' \item \emph{ @param } treasurer.name list( character )
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } candidate.id list( character )
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } party list( character )
#' \item \emph{ @param } min.last.f1.date character
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{CommitteePage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : CommitteePage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  CandidateCandidateIdCommitteesGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.candidate.id <- 'candidate.id_example' # character |  A unique identifier assigned to each candidate registered with the FEC. If a person runs for several offices, that person will have separate candidate IDs for each office. 
#' var.designation <- ['designation_example'] # array[character] | The one-letter designation code of the organization:          - A authorized by a candidate          - J joint fundraising committee          - P principal campaign committee of a candidate          - U unauthorized          - B lobbyist/registrant PAC          - D leadership PAC 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.year <- [56] # array[integer] | A year that the committee was active— (after original registration date     or filing but before expiration date)
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.organization.type <- ['organization.type_example'] # array[character] | The one-letter code for the kind for organization:         - C corporation         - L labor organization         - M membership organization         - T trade association         - V cooperative         - W corporation without capital stock 
#' var.committee.type <- ['committee.type_example'] # array[character] | The one-letter type code of the organization:         - C communication cost         - D delegate         - E electioneering communication         - H House         - I independent expenditor (person or group)         - N PAC - nonqualified         - O independent expenditure-only (super PACs)         - P presidential         - Q PAC - qualified         - S Senate         - U single candidate independent expenditure         - V PAC with non-contribution account, nonqualified         - W PAC with non-contribution account, qualified         - X party, nonqualified         - Y party, qualified         - Z national party non-federal account 
#' var.sort <- 'name' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.filing.frequency <- ['filing.frequency_example'] # array[character] | The one-letter      code of the filing frequency:          - A Administratively terminated          - D Debt          - M Monthly filer          - Q Quarterly filer          - T Terminated          - W Waived 
#' var.cycle <- [56] # array[integer] |  A two year election cycle that the committee was active- (after original registration date but before expiration date in Form 1s) The cycle begins with an odd year and is named for its ending, even year. 
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- CommitteeApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$CandidateCandidateIdCommitteesGet(var.api.key, var.candidate.id, designation=var.designation, sort.hide.null=var.sort.hide.null, year=var.year, per.page=var.per.page, sort.nulls.last=var.sort.nulls.last, organization.type=var.organization.type, committee.type=var.committee.type, sort=var.sort, sort.null.only=var.sort.null.only, filing.frequency=var.filing.frequency, cycle=var.cycle, page=var.page)
#'
#'
#' ####################  CandidateCandidateIdCommitteesHistoryCycleGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.cycle <- 56 # integer |  A two year election cycle that the committee was active- (after original registration date but before expiration date in Form 1s) The cycle begins with an odd year and is named for its ending, even year. 
#' var.candidate.id <- 'candidate.id_example' # character |  A unique identifier assigned to each candidate registered with the FEC. If a person runs for several offices, that person will have separate candidate IDs for each office. 
#' var.designation <- ['designation_example'] # array[character] | The one-letter designation code of the organization:          - A authorized by a candidate          - J joint fundraising committee          - P principal campaign committee of a candidate          - U unauthorized          - B lobbyist/registrant PAC          - D leadership PAC 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.election.full <- TRUE # character | `True` indicates that full election period of a candidate. `False` indicates that two year election cycle.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.sort <- '-cycle' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- CommitteeApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$CandidateCandidateIdCommitteesHistoryCycleGet(var.api.key, var.cycle, var.candidate.id, designation=var.designation, sort.hide.null=var.sort.hide.null, per.page=var.per.page, election.full=var.election.full, sort.nulls.last=var.sort.nulls.last, sort.null.only=var.sort.null.only, sort=var.sort, page=var.page)
#'
#'
#' ####################  CandidateCandidateIdCommitteesHistoryGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.candidate.id <- 'candidate.id_example' # character |  A unique identifier assigned to each candidate registered with the FEC. If a person runs for several offices, that person will have separate candidate IDs for each office. 
#' var.designation <- ['designation_example'] # array[character] | The one-letter designation code of the organization:          - A authorized by a candidate          - J joint fundraising committee          - P principal campaign committee of a candidate          - U unauthorized          - B lobbyist/registrant PAC          - D leadership PAC 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.election.full <- TRUE # character | `True` indicates that full election period of a candidate. `False` indicates that two year election cycle.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.sort <- '-cycle' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- CommitteeApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$CandidateCandidateIdCommitteesHistoryGet(var.api.key, var.candidate.id, designation=var.designation, sort.hide.null=var.sort.hide.null, per.page=var.per.page, election.full=var.election.full, sort.nulls.last=var.sort.nulls.last, sort.null.only=var.sort.null.only, sort=var.sort, page=var.page)
#'
#'
#' ####################  CommitteeCommitteeIdGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.committee.id <- 'committee.id_example' # character |  A unique identifier assigned to each committee or filer registered with the FEC. In general committee id's begin with the letter C which is followed by eight digits. 
#' var.designation <- ['designation_example'] # array[character] | The one-letter designation code of the organization:          - A authorized by a candidate          - J joint fundraising committee          - P principal campaign committee of a candidate          - U unauthorized          - B lobbyist/registrant PAC          - D leadership PAC 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.year <- [56] # array[integer] | A year that the committee was active— (after original registration date     or filing but before expiration date)
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.organization.type <- ['organization.type_example'] # array[character] | The one-letter code for the kind for organization:         - C corporation         - L labor organization         - M membership organization         - T trade association         - V cooperative         - W corporation without capital stock 
#' var.committee.type <- ['committee.type_example'] # array[character] | The one-letter type code of the organization:         - C communication cost         - D delegate         - E electioneering communication         - H House         - I independent expenditor (person or group)         - N PAC - nonqualified         - O independent expenditure-only (super PACs)         - P presidential         - Q PAC - qualified         - S Senate         - U single candidate independent expenditure         - V PAC with non-contribution account, nonqualified         - W PAC with non-contribution account, qualified         - X party, nonqualified         - Y party, qualified         - Z national party non-federal account 
#' var.sort <- 'name' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.filing.frequency <- ['filing.frequency_example'] # array[character] | The one-letter      code of the filing frequency:          - A Administratively terminated          - D Debt          - M Monthly filer          - Q Quarterly filer          - T Terminated          - W Waived 
#' var.cycle <- [56] # array[integer] |  A two year election cycle that the committee was active- (after original registration date but before expiration date in Form 1s) The cycle begins with an odd year and is named for its ending, even year. 
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- CommitteeApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$CommitteeCommitteeIdGet(var.api.key, var.committee.id, designation=var.designation, sort.hide.null=var.sort.hide.null, year=var.year, per.page=var.per.page, sort.nulls.last=var.sort.nulls.last, organization.type=var.organization.type, committee.type=var.committee.type, sort=var.sort, sort.null.only=var.sort.null.only, filing.frequency=var.filing.frequency, cycle=var.cycle, page=var.page)
#'
#'
#' ####################  CommitteeCommitteeIdHistoryCycleGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.cycle <- 56 # integer |  A two year election cycle that the committee was active- (after original registration date but before expiration date in Form 1s) The cycle begins with an odd year and is named for its ending, even year. 
#' var.committee.id <- 'committee.id_example' # character |  A unique identifier assigned to each committee or filer registered with the FEC. In general committee id's begin with the letter C which is followed by eight digits. 
#' var.designation <- ['designation_example'] # array[character] | The one-letter designation code of the organization:          - A authorized by a candidate          - J joint fundraising committee          - P principal campaign committee of a candidate          - U unauthorized          - B lobbyist/registrant PAC          - D leadership PAC 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.election.full <- TRUE # character | `True` indicates that full election period of a candidate. `False` indicates that two year election cycle.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.sort <- '-cycle' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- CommitteeApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$CommitteeCommitteeIdHistoryCycleGet(var.api.key, var.cycle, var.committee.id, designation=var.designation, sort.hide.null=var.sort.hide.null, per.page=var.per.page, election.full=var.election.full, sort.nulls.last=var.sort.nulls.last, sort.null.only=var.sort.null.only, sort=var.sort, page=var.page)
#'
#'
#' ####################  CommitteeCommitteeIdHistoryGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.committee.id <- 'committee.id_example' # character |  A unique identifier assigned to each committee or filer registered with the FEC. In general committee id's begin with the letter C which is followed by eight digits. 
#' var.designation <- ['designation_example'] # array[character] | The one-letter designation code of the organization:          - A authorized by a candidate          - J joint fundraising committee          - P principal campaign committee of a candidate          - U unauthorized          - B lobbyist/registrant PAC          - D leadership PAC 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.election.full <- TRUE # character | `True` indicates that full election period of a candidate. `False` indicates that two year election cycle.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.sort <- '-cycle' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- CommitteeApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$CommitteeCommitteeIdHistoryGet(var.api.key, var.committee.id, designation=var.designation, sort.hide.null=var.sort.hide.null, per.page=var.per.page, election.full=var.election.full, sort.nulls.last=var.sort.nulls.last, sort.null.only=var.sort.null.only, sort=var.sort, page=var.page)
#'
#'
#' ####################  CommitteesGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.designation <- ['designation_example'] # array[character] | The one-letter designation code of the organization:          - A authorized by a candidate          - J joint fundraising committee          - P principal campaign committee of a candidate          - U unauthorized          - B lobbyist/registrant PAC          - D leadership PAC 
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.committee.id <- ['committee.id_example'] # array[character] |  A unique identifier assigned to each committee or filer registered with the FEC. In general committee id's begin with the letter C which is followed by eight digits. 
#' var.organization.type <- ['organization.type_example'] # array[character] | The one-letter code for the kind for organization:         - C corporation         - L labor organization         - M membership organization         - T trade association         - V cooperative         - W corporation without capital stock 
#' var.committee.type <- ['committee.type_example'] # array[character] | The one-letter type code of the organization:         - C communication cost         - D delegate         - E electioneering communication         - H House         - I independent expenditor (person or group)         - N PAC - nonqualified         - O independent expenditure-only (super PACs)         - P presidential         - Q PAC - qualified         - S Senate         - U single candidate independent expenditure         - V PAC with non-contribution account, nonqualified         - W PAC with non-contribution account, qualified         - X party, nonqualified         - Y party, qualified         - Z national party non-federal account 
#' var.sort <- 'name' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.max.last.f1.date <- 'max.last.f1.date_example' # character | Filter for committees whose latest Form 1 was received on or before this date.
#' var.q <- ['q_example'] # array[character] | The name of the committee. If a committee changes its name,     the most recent name will be shown. Committee names are not unique. Use committee_id     for looking up records.
#' var.filing.frequency <- ['filing.frequency_example'] # array[character] | The one-letter      code of the filing frequency:          - A Administratively terminated          - D Debt          - M Monthly filer          - Q Quarterly filer          - T Terminated          - W Waived 
#' var.state <- ['state_example'] # array[character] | US state or territory
#' var.cycle <- [56] # array[integer] |  A two year election cycle that the committee was active- (after original registration date but before expiration date in Form 1s) The cycle begins with an odd year and is named for its ending, even year. 
#' var.min.first.file.date <- 'min.first.file.date_example' # character | Filter for committees whose first filing was received on or after this date.
#' var.sponsor.candidate.id <- ['sponsor.candidate.id_example'] # array[character] |  A unique identifier assigned to each candidate registered with the FEC. If a person runs for several offices, that person will have separate candidate IDs for each office. This is a filter for Leadership PAC sponsor. 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.year <- [56] # array[integer] | A year that the committee was active— (after original registration date     or filing but before expiration date)
#' var.max.first.file.date <- 'max.first.file.date_example' # character | Filter for committees whose first filing was received on or before this date.
#' var.treasurer.name <- ['treasurer.name_example'] # array[character] | Name of the Committee's treasurer. If multiple treasurers for the committee, the most recent treasurer will be shown.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.candidate.id <- ['candidate.id_example'] # array[character] |  A unique identifier assigned to each candidate registered with the FEC. If a person runs for several offices, that person will have separate candidate IDs for each office. 
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.party <- ['party_example'] # array[character] | Three-letter code for the party affiliated with a candidate or committee. For example, DEM for Democratic Party and REP for Republican Party.
#' var.min.last.f1.date <- 'min.last.f1.date_example' # character | Filter for committees whose latest Form 1 was received on or after this date.
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- CommitteeApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$CommitteesGet(var.api.key, designation=var.designation, per.page=var.per.page, committee.id=var.committee.id, organization.type=var.organization.type, committee.type=var.committee.type, sort=var.sort, max.last.f1.date=var.max.last.f1.date, q=var.q, filing.frequency=var.filing.frequency, state=var.state, cycle=var.cycle, min.first.file.date=var.min.first.file.date, sponsor.candidate.id=var.sponsor.candidate.id, sort.hide.null=var.sort.hide.null, year=var.year, max.first.file.date=var.max.first.file.date, treasurer.name=var.treasurer.name, sort.nulls.last=var.sort.nulls.last, candidate.id=var.candidate.id, sort.null.only=var.sort.null.only, party=var.party, min.last.f1.date=var.min.last.f1.date, page=var.page)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
CommitteeApi <- R6::R6Class(
  'CommitteeApi',
  public = list(
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    CandidateCandidateIdCommitteesGet = function(api.key, candidate.id, designation=NULL, sort.hide.null=FALSE, year=NULL, per.page=20, sort.nulls.last=FALSE, organization.type=NULL, committee.type=NULL, sort='name', sort.null.only=FALSE, filing.frequency=NULL, cycle=NULL, page=1, ...){
      apiResponse <- self$CandidateCandidateIdCommitteesGetWithHttpInfo(api.key, candidate.id, designation, sort.hide.null, year, per.page, sort.nulls.last, organization.type, committee.type, sort, sort.null.only, filing.frequency, cycle, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    CandidateCandidateIdCommitteesGetWithHttpInfo = function(api.key, candidate.id, designation=NULL, sort.hide.null=FALSE, year=NULL, per.page=20, sort.nulls.last=FALSE, organization.type=NULL, committee.type=NULL, sort='name', sort.null.only=FALSE, filing.frequency=NULL, cycle=NULL, page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      if (missing(`candidate.id`)) {
        stop("Missing required parameter `candidate.id`.")
      }

      queryParams['designation'] <- designation

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['year'] <- year

      queryParams['per_page'] <- per.page

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['organization_type'] <- organization.type

      queryParams['committee_type'] <- committee.type

      queryParams['sort'] <- sort

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['filing_frequency'] <- filing.frequency

      queryParams['cycle'] <- cycle

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/candidate/{candidate_id}/committees/"
      if (!missing(`candidate.id`)) {
        urlPath <- gsub(paste0("\\{", "candidate_id", "\\}"), URLencode(as.character(`candidate.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "CommitteeDetailPage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    CandidateCandidateIdCommitteesHistoryCycleGet = function(api.key, cycle, candidate.id, designation=NULL, sort.hide.null=FALSE, per.page=20, election.full=TRUE, sort.nulls.last=FALSE, sort.null.only=FALSE, sort='-cycle', page=1, ...){
      apiResponse <- self$CandidateCandidateIdCommitteesHistoryCycleGetWithHttpInfo(api.key, cycle, candidate.id, designation, sort.hide.null, per.page, election.full, sort.nulls.last, sort.null.only, sort, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    CandidateCandidateIdCommitteesHistoryCycleGetWithHttpInfo = function(api.key, cycle, candidate.id, designation=NULL, sort.hide.null=FALSE, per.page=20, election.full=TRUE, sort.nulls.last=FALSE, sort.null.only=FALSE, sort='-cycle', page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      if (missing(`cycle`)) {
        stop("Missing required parameter `cycle`.")
      }

      if (missing(`candidate.id`)) {
        stop("Missing required parameter `candidate.id`.")
      }

      queryParams['designation'] <- designation

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['per_page'] <- per.page

      queryParams['election_full'] <- election.full

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['sort'] <- sort

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/candidate/{candidate_id}/committees/history/{cycle}/"
      if (!missing(`cycle`)) {
        urlPath <- gsub(paste0("\\{", "cycle", "\\}"), URLencode(as.character(`cycle`), reserved = TRUE), urlPath)
      }

      if (!missing(`candidate.id`)) {
        urlPath <- gsub(paste0("\\{", "candidate_id", "\\}"), URLencode(as.character(`candidate.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "CommitteeHistoryPage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    CandidateCandidateIdCommitteesHistoryGet = function(api.key, candidate.id, designation=NULL, sort.hide.null=FALSE, per.page=20, election.full=TRUE, sort.nulls.last=FALSE, sort.null.only=FALSE, sort='-cycle', page=1, ...){
      apiResponse <- self$CandidateCandidateIdCommitteesHistoryGetWithHttpInfo(api.key, candidate.id, designation, sort.hide.null, per.page, election.full, sort.nulls.last, sort.null.only, sort, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    CandidateCandidateIdCommitteesHistoryGetWithHttpInfo = function(api.key, candidate.id, designation=NULL, sort.hide.null=FALSE, per.page=20, election.full=TRUE, sort.nulls.last=FALSE, sort.null.only=FALSE, sort='-cycle', page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      if (missing(`candidate.id`)) {
        stop("Missing required parameter `candidate.id`.")
      }

      queryParams['designation'] <- designation

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['per_page'] <- per.page

      queryParams['election_full'] <- election.full

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['sort'] <- sort

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/candidate/{candidate_id}/committees/history/"
      if (!missing(`candidate.id`)) {
        urlPath <- gsub(paste0("\\{", "candidate_id", "\\}"), URLencode(as.character(`candidate.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "CommitteeHistoryPage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    CommitteeCommitteeIdGet = function(api.key, committee.id, designation=NULL, sort.hide.null=FALSE, year=NULL, per.page=20, sort.nulls.last=FALSE, organization.type=NULL, committee.type=NULL, sort='name', sort.null.only=FALSE, filing.frequency=NULL, cycle=NULL, page=1, ...){
      apiResponse <- self$CommitteeCommitteeIdGetWithHttpInfo(api.key, committee.id, designation, sort.hide.null, year, per.page, sort.nulls.last, organization.type, committee.type, sort, sort.null.only, filing.frequency, cycle, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    CommitteeCommitteeIdGetWithHttpInfo = function(api.key, committee.id, designation=NULL, sort.hide.null=FALSE, year=NULL, per.page=20, sort.nulls.last=FALSE, organization.type=NULL, committee.type=NULL, sort='name', sort.null.only=FALSE, filing.frequency=NULL, cycle=NULL, page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      if (missing(`committee.id`)) {
        stop("Missing required parameter `committee.id`.")
      }

      queryParams['designation'] <- designation

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['year'] <- year

      queryParams['per_page'] <- per.page

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['organization_type'] <- organization.type

      queryParams['committee_type'] <- committee.type

      queryParams['sort'] <- sort

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['filing_frequency'] <- filing.frequency

      queryParams['cycle'] <- cycle

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/committee/{committee_id}/"
      if (!missing(`committee.id`)) {
        urlPath <- gsub(paste0("\\{", "committee_id", "\\}"), URLencode(as.character(`committee.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "CommitteeDetailPage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    CommitteeCommitteeIdHistoryCycleGet = function(api.key, cycle, committee.id, designation=NULL, sort.hide.null=FALSE, per.page=20, election.full=TRUE, sort.nulls.last=FALSE, sort.null.only=FALSE, sort='-cycle', page=1, ...){
      apiResponse <- self$CommitteeCommitteeIdHistoryCycleGetWithHttpInfo(api.key, cycle, committee.id, designation, sort.hide.null, per.page, election.full, sort.nulls.last, sort.null.only, sort, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    CommitteeCommitteeIdHistoryCycleGetWithHttpInfo = function(api.key, cycle, committee.id, designation=NULL, sort.hide.null=FALSE, per.page=20, election.full=TRUE, sort.nulls.last=FALSE, sort.null.only=FALSE, sort='-cycle', page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      if (missing(`cycle`)) {
        stop("Missing required parameter `cycle`.")
      }

      if (missing(`committee.id`)) {
        stop("Missing required parameter `committee.id`.")
      }

      queryParams['designation'] <- designation

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['per_page'] <- per.page

      queryParams['election_full'] <- election.full

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['sort'] <- sort

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/committee/{committee_id}/history/{cycle}/"
      if (!missing(`cycle`)) {
        urlPath <- gsub(paste0("\\{", "cycle", "\\}"), URLencode(as.character(`cycle`), reserved = TRUE), urlPath)
      }

      if (!missing(`committee.id`)) {
        urlPath <- gsub(paste0("\\{", "committee_id", "\\}"), URLencode(as.character(`committee.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "CommitteeHistoryPage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    CommitteeCommitteeIdHistoryGet = function(api.key, committee.id, designation=NULL, sort.hide.null=FALSE, per.page=20, election.full=TRUE, sort.nulls.last=FALSE, sort.null.only=FALSE, sort='-cycle', page=1, ...){
      apiResponse <- self$CommitteeCommitteeIdHistoryGetWithHttpInfo(api.key, committee.id, designation, sort.hide.null, per.page, election.full, sort.nulls.last, sort.null.only, sort, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    CommitteeCommitteeIdHistoryGetWithHttpInfo = function(api.key, committee.id, designation=NULL, sort.hide.null=FALSE, per.page=20, election.full=TRUE, sort.nulls.last=FALSE, sort.null.only=FALSE, sort='-cycle', page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      if (missing(`committee.id`)) {
        stop("Missing required parameter `committee.id`.")
      }

      queryParams['designation'] <- designation

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['per_page'] <- per.page

      queryParams['election_full'] <- election.full

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['sort'] <- sort

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/committee/{committee_id}/history/"
      if (!missing(`committee.id`)) {
        urlPath <- gsub(paste0("\\{", "committee_id", "\\}"), URLencode(as.character(`committee.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "CommitteeHistoryPage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    CommitteesGet = function(api.key, designation=NULL, per.page=20, committee.id=NULL, organization.type=NULL, committee.type=NULL, sort='name', max.last.f1.date=NULL, q=NULL, filing.frequency=NULL, state=NULL, cycle=NULL, min.first.file.date=NULL, sponsor.candidate.id=NULL, sort.hide.null=FALSE, year=NULL, max.first.file.date=NULL, treasurer.name=NULL, sort.nulls.last=FALSE, candidate.id=NULL, sort.null.only=FALSE, party=NULL, min.last.f1.date=NULL, page=1, ...){
      apiResponse <- self$CommitteesGetWithHttpInfo(api.key, designation, per.page, committee.id, organization.type, committee.type, sort, max.last.f1.date, q, filing.frequency, state, cycle, min.first.file.date, sponsor.candidate.id, sort.hide.null, year, max.first.file.date, treasurer.name, sort.nulls.last, candidate.id, sort.null.only, party, min.last.f1.date, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    CommitteesGetWithHttpInfo = function(api.key, designation=NULL, per.page=20, committee.id=NULL, organization.type=NULL, committee.type=NULL, sort='name', max.last.f1.date=NULL, q=NULL, filing.frequency=NULL, state=NULL, cycle=NULL, min.first.file.date=NULL, sponsor.candidate.id=NULL, sort.hide.null=FALSE, year=NULL, max.first.file.date=NULL, treasurer.name=NULL, sort.nulls.last=FALSE, candidate.id=NULL, sort.null.only=FALSE, party=NULL, min.last.f1.date=NULL, page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['designation'] <- designation

      queryParams['per_page'] <- per.page

      queryParams['committee_id'] <- committee.id

      queryParams['organization_type'] <- organization.type

      queryParams['committee_type'] <- committee.type

      queryParams['sort'] <- sort

      queryParams['max_last_f1_date'] <- max.last.f1.date

      queryParams['q'] <- q

      queryParams['filing_frequency'] <- filing.frequency

      queryParams['state'] <- state

      queryParams['cycle'] <- cycle

      queryParams['min_first_file_date'] <- min.first.file.date

      queryParams['sponsor_candidate_id'] <- sponsor.candidate.id

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['year'] <- year

      queryParams['max_first_file_date'] <- max.first.file.date

      queryParams['treasurer_name'] <- treasurer.name

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['candidate_id'] <- candidate.id

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['party'] <- party

      queryParams['min_last_f1_date'] <- min.last.f1.date

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/committees/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "CommitteePage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    }
  )
)
