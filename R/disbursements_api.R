# OpenFEC
#
# This application programming interface (API) allows you to explore the way candidates and committees fund their campaigns.    The Federal Election Commission (FEC) API is a RESTful web service supporting full-text and field-specific searches on FEC data. [Bulk downloads](https://www.fec.gov/data/advanced/?tab=bulk-data) are available on the current site. Information is tied to the underlying forms by file ID and image ID. Data is updated nightly.    There are a lot of data, and a good place to start is to use search to find interesting candidates and committees. Then, you can use their IDs to find report or line item details with the other endpoints. If you are interested in individual donors, check out contributor information in the `/schedule_a/` endpoints.    <b class=\"body\" id=\"getting_started_head\">Getting started with the openFEC API</b><br>    If you would like to use the FEC's API programmatically, you can sign up for your own API key using our form. Alternatively, you can still try out our API without an API key by using the web interface and using DEMO_KEY. Note that when you use the openFEC API you are subject to the [Terms of Service](https://github.com/fecgov/FEC/blob/master/TERMS-OF-SERVICE.md) and [Acceptable Use policy](https://github.com/fecgov/FEC/blob/master/ACCEPTABLE-USE-POLICY.md).    Signing up for an API key will enable you to place up to 1,000 calls an hour. Each call is limited to 100 results per page. You can email questions, comments or a request to get a key for 7,200 calls an hour (120 calls per minute) to <a href=\"mailto:APIinfo@fec.gov\">APIinfo@fec.gov</a>. You can also ask questions and discuss the data in a community led [group](https://groups.google.com/forum/#!forum/fec-data).    The model definitions and schema are available at [/swagger](/swagger/). This is useful for making wrappers and exploring the data.    A few restrictions limit the way you can use FEC data. For example, you can’t use contributor lists for commercial purposes or to solicit donations. [Learn more here](https://www.fec.gov/updates/sale-or-use-contributor-information/).    [Inspect our source code](https://github.com/fecgov/openFEC). We welcome issues and pull requests!    <p><br></p> <h2 class=\"title\" id=\"signup_head\">Sign up for an API key</h2> <div id=\"apidatagov_signup\">Loading signup form...</div>
#
# The version of the OpenAPI document: 1.0
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title Disbursements operations
#' @description openapi.Disbursements
#' @format An \code{R6Class} generator object
#' @field apiClient Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ SchedulesScheduleBByPurposeGet } \emph{  }
#'  Schedule B disbursements aggregated by disbursement purpose category. To avoid double counting, memoed items are not included. Purpose is a combination of transaction codes, category codes and disbursement description. Inspect the &#x60;disbursement_purpose&#x60; sql function within the migrations for more details. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } purpose list( character )
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } committee.id list( character )
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } sort character
#' \item \emph{ @param } cycle list( integer )
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{ScheduleBByPurposePage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : ScheduleBByPurposePage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SchedulesScheduleBByRecipientGet } \emph{  }
#'  Schedule B disbursements aggregated by recipient name. To avoid double counting, memoed items are not included. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } recipient.name list( character )
#' \item \emph{ @param } committee.id list( character )
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } sort character
#' \item \emph{ @param } cycle list( integer )
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{ScheduleBByRecipientPage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : ScheduleBByRecipientPage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SchedulesScheduleBByRecipientIdGet } \emph{  }
#'  Schedule B disbursements aggregated by recipient committee ID, if applicable. To avoid double counting, memoed items are not included. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } committee.id list( character )
#' \item \emph{ @param } recipient.id list( character )
#' \item \emph{ @param } sort character
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } cycle list( integer )
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{ScheduleBByRecipientIDPage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : ScheduleBByRecipientIDPage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SchedulesScheduleBEfileGet } \emph{  }
#'  Efiling endpoints provide real-time campaign finance data received from electronic filers. Efiling endpoints only contain the most recent four months of data and don&#39;t contain the processed and coded data that you can find on other endpoints. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } min.date character
#' \item \emph{ @param } max.amount character
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } max.date character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } image.number list( character )
#' \item \emph{ @param } recipient.state list( character )
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } committee.id list( character )
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } sort character
#' \item \emph{ @param } page integer
#' \item \emph{ @param } recipient.city list( character )
#' \item \emph{ @param } min.amount character
#' \item \emph{ @param } disbursement.description list( character )
#' \item \emph{ @returnType } \link{ScheduleBEfilePage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : ScheduleBEfilePage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SchedulesScheduleBGet } \emph{  }
#'  Schedule B filings describe itemized disbursements. This data explains how committees and other filers spend their money. These figures are reported as part of forms F3, F3X and F3P.  The data is divided in two-year periods, called &#x60;two_year_transaction_period&#x60;, which is derived from the &#x60;report_year&#x60; submitted of the corresponding form. If no value is supplied, the results will default to the most recent two-year period that is named after the ending, even-numbered year.  Due to the large quantity of Schedule B filings, this endpoint is not paginated by page number. Instead, you can request the next page of results by adding the values in the &#x60;last_indexes&#x60; object from &#x60;pagination&#x60; to the URL of your last request. For example, when sorting by &#x60;disbursement_date&#x60;, you might receive a page of results with the following pagination information:  &#x60;&#x60;&#x60; pagination: {     pages: 965191,     per_page: 20,     count: 19303814,     last_indexes: {         last_index: \&quot;230906248\&quot;,         last_disbursement_date: \&quot;2014-07-04\&quot;     } } &#x60;&#x60;&#x60;  To fetch the next page of sorted results, append &#x60;last_index&#x3D;230906248&#x60; and &#x60;last_disbursement_date&#x3D;2014-07-04&#x60; to the URL.  We strongly advise paging through these results by using the sort indices (defaults to sort by disbursement date, e.g. &#x60;last_disbursement_date&#x60;), otherwise some resources may be unintentionally filtered out. This resource uses keyset pagination to improve query performance and these indices are required to properly page through this large dataset.  Note: because the Schedule B data includes many records, counts for large result sets are approximate; you will want to page through the records until no records are returned. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } last.disbursement.date character
#' \item \emph{ @param } max.image.number character
#' \item \emph{ @param } min.date character
#' \item \emph{ @param } max.amount character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } line.number character
#' \item \emph{ @param } recipient.state list( character )
#' \item \emph{ @param } recipient.name list( character )
#' \item \emph{ @param } committee.id list( character )
#' \item \emph{ @param } sort character
#' \item \emph{ @param } recipient.city list( character )
#' \item \emph{ @param } spender.committee.type Enum < [, C, D, E, H, I, N, O, P, Q, S, U, V, W, X, Y, Z] > 
#' \item \emph{ @param } min.amount character
#' \item \emph{ @param } last.index integer
#' \item \emph{ @param } disbursement.description list( character )
#' \item \emph{ @param } disbursement.purpose.category list( character )
#' \item \emph{ @param } min.image.number character
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } last.disbursement.amount numeric
#' \item \emph{ @param } max.date character
#' \item \emph{ @param } image.number list( character )
#' \item \emph{ @param } recipient.committee.id list( character )
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } spender.committee.designation Enum < [, A, J, P, U, B, D] > 
#' \item \emph{ @param } two.year.transaction.period list( integer )
#' \item \emph{ @param } spender.committee.org.type Enum < [, C, L, M, T, V, W] > 
#' \item \emph{ @returnType } \link{ScheduleBPage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : ScheduleBPage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SchedulesScheduleBSubIdGet } \emph{  }
#'  Schedule B filings describe itemized disbursements. This data explains how committees and other filers spend their money. These figures are reported as part of forms F3, F3X and F3P.  The data is divided in two-year periods, called &#x60;two_year_transaction_period&#x60;, which is derived from the &#x60;report_year&#x60; submitted of the corresponding form. If no value is supplied, the results will default to the most recent two-year period that is named after the ending, even-numbered year.  Due to the large quantity of Schedule B filings, this endpoint is not paginated by page number. Instead, you can request the next page of results by adding the values in the &#x60;last_indexes&#x60; object from &#x60;pagination&#x60; to the URL of your last request. For example, when sorting by &#x60;disbursement_date&#x60;, you might receive a page of results with the following pagination information:  &#x60;&#x60;&#x60; pagination: {     pages: 965191,     per_page: 20,     count: 19303814,     last_indexes: {         last_index: \&quot;230906248\&quot;,         last_disbursement_date: \&quot;2014-07-04\&quot;     } } &#x60;&#x60;&#x60;  To fetch the next page of sorted results, append &#x60;last_index&#x3D;230906248&#x60; and &#x60;last_disbursement_date&#x3D;2014-07-04&#x60; to the URL.  We strongly advise paging through these results by using the sort indices (defaults to sort by disbursement date, e.g. &#x60;last_disbursement_date&#x60;), otherwise some resources may be unintentionally filtered out. This resource uses keyset pagination to improve query performance and these indices are required to properly page through this large dataset.  Note: because the Schedule B data includes many records, counts for large result sets are approximate; you will want to page through the records until no records are returned. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } sub.id character
#' \item \emph{ @param } last.disbursement.date character
#' \item \emph{ @param } max.image.number character
#' \item \emph{ @param } min.date character
#' \item \emph{ @param } max.amount character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } line.number character
#' \item \emph{ @param } recipient.state list( character )
#' \item \emph{ @param } recipient.name list( character )
#' \item \emph{ @param } committee.id list( character )
#' \item \emph{ @param } sort character
#' \item \emph{ @param } recipient.city list( character )
#' \item \emph{ @param } spender.committee.type Enum < [, C, D, E, H, I, N, O, P, Q, S, U, V, W, X, Y, Z] > 
#' \item \emph{ @param } min.amount character
#' \item \emph{ @param } last.index integer
#' \item \emph{ @param } disbursement.description list( character )
#' \item \emph{ @param } disbursement.purpose.category list( character )
#' \item \emph{ @param } min.image.number character
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } last.disbursement.amount numeric
#' \item \emph{ @param } max.date character
#' \item \emph{ @param } image.number list( character )
#' \item \emph{ @param } recipient.committee.id list( character )
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } spender.committee.designation Enum < [, A, J, P, U, B, D] > 
#' \item \emph{ @param } two.year.transaction.period list( integer )
#' \item \emph{ @param } spender.committee.org.type Enum < [, C, L, M, T, V, W] > 
#' \item \emph{ @returnType } \link{ScheduleBPage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : ScheduleBPage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  SchedulesScheduleBByPurposeGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.purpose <- ['purpose_example'] # array[character] | Disbursement purpose category
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.committee.id <- ['committee.id_example'] # array[character] |  A unique identifier assigned to each committee or filer registered with the FEC. In general committee id's begin with the letter C which is followed by eight digits. 
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.sort <- 'null' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.cycle <- [56] # array[integer] |  Filter records to only those that were applicable to a given two-year period.The cycle begins with an odd year and is named for its ending, even year. 
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- DisbursementsApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$SchedulesScheduleBByPurposeGet(var.api.key, sort.hide.null=var.sort.hide.null, purpose=var.purpose, per.page=var.per.page, sort.nulls.last=var.sort.nulls.last, committee.id=var.committee.id, sort.null.only=var.sort.null.only, sort=var.sort, cycle=var.cycle, page=var.page)
#'
#'
#' ####################  SchedulesScheduleBByRecipientGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.recipient.name <- ['recipient.name_example'] # array[character] | Name of the entity receiving the disbursement
#' var.committee.id <- ['committee.id_example'] # array[character] |  A unique identifier assigned to each committee or filer registered with the FEC. In general committee id's begin with the letter C which is followed by eight digits. 
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.sort <- 'null' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.cycle <- [56] # array[integer] |  Filter records to only those that were applicable to a given two-year period.The cycle begins with an odd year and is named for its ending, even year. 
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- DisbursementsApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$SchedulesScheduleBByRecipientGet(var.api.key, sort.hide.null=var.sort.hide.null, per.page=var.per.page, sort.nulls.last=var.sort.nulls.last, recipient.name=var.recipient.name, committee.id=var.committee.id, sort.null.only=var.sort.null.only, sort=var.sort, cycle=var.cycle, page=var.page)
#'
#'
#' ####################  SchedulesScheduleBByRecipientIdGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.committee.id <- ['committee.id_example'] # array[character] |  A unique identifier assigned to each committee or filer registered with the FEC. In general committee id's begin with the letter C which is followed by eight digits. 
#' var.recipient.id <- ['recipient.id_example'] # array[character] | The FEC identifier should be represented here if the entity receiving the disbursement is registered with the FEC.
#' var.sort <- 'null' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.cycle <- [56] # array[integer] |  Filter records to only those that were applicable to a given two-year period.The cycle begins with an odd year and is named for its ending, even year. 
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- DisbursementsApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$SchedulesScheduleBByRecipientIdGet(var.api.key, sort.hide.null=var.sort.hide.null, per.page=var.per.page, sort.nulls.last=var.sort.nulls.last, committee.id=var.committee.id, recipient.id=var.recipient.id, sort=var.sort, sort.null.only=var.sort.null.only, cycle=var.cycle, page=var.page)
#'
#'
#' ####################  SchedulesScheduleBEfileGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.min.date <- 'min.date_example' # character | When sorting by `disbursement_date`, this is populated with the         `disbursement_date` of the last result. However, you will need to pass the index         of that last result to `last_index` to get the next page.
#' var.max.amount <- 'max.amount_example' # character | Filter for all amounts less than a value.
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.max.date <- 'max.date_example' # character | When sorting by `disbursement_date`, this is populated with the         `disbursement_date` of the last result. However, you will need to pass the index         of that last result to `last_index` to get the next page.
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.image.number <- ['image.number_example'] # array[character] | The image number of the page where the schedule item is reported
#' var.recipient.state <- ['recipient.state_example'] # array[character] | State of recipient
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.committee.id <- ['committee.id_example'] # array[character] |  A unique identifier assigned to each committee or filer registered with the FEC. In general committee id's begin with the letter C which is followed by eight digits. 
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.sort <- '-disbursement_date' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#' var.recipient.city <- ['recipient.city_example'] # array[character] | City of recipient
#' var.min.amount <- 'min.amount_example' # character | Filter for all amounts less than a value.
#' var.disbursement.description <- ['disbursement.description_example'] # array[character] | Description of disbursement
#'
#' api.instance <- DisbursementsApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$SchedulesScheduleBEfileGet(var.api.key, min.date=var.min.date, max.amount=var.max.amount, sort.hide.null=var.sort.hide.null, max.date=var.max.date, per.page=var.per.page, image.number=var.image.number, recipient.state=var.recipient.state, sort.nulls.last=var.sort.nulls.last, committee.id=var.committee.id, sort.null.only=var.sort.null.only, sort=var.sort, page=var.page, recipient.city=var.recipient.city, min.amount=var.min.amount, disbursement.description=var.disbursement.description)
#'
#'
#' ####################  SchedulesScheduleBGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.last.disbursement.date <- 'last.disbursement.date_example' # character | When sorting by `disbursement_date`, this is populated with the `disbursement_date` of the last result. However, you will need to pass the index of that last result to `last_index` to get the next page.
#' var.max.image.number <- 'max.image.number_example' # character | 
#' var.min.date <- 'min.date_example' # character | Minimum date
#' var.max.amount <- 'max.amount_example' # character | Filter for all amounts less than a value.
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.line.number <- 'line.number_example' # character | Filter for form and line number using the following format: `FORM-LINENUMBER`.  For example an argument such as `F3X-16` would filter down to all entries from form `F3X` line number `16`.
#' var.recipient.state <- ['recipient.state_example'] # array[character] | State of recipient
#' var.recipient.name <- ['recipient.name_example'] # array[character] | Name of the entity receiving the disbursement
#' var.committee.id <- ['committee.id_example'] # array[character] |  A unique identifier assigned to each committee or filer registered with the FEC. In general committee id's begin with the letter C which is followed by eight digits. 
#' var.sort <- '-disbursement_date' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.recipient.city <- ['recipient.city_example'] # array[character] | City of recipient
#' var.spender.committee.type <- ['spender.committee.type_example'] # array[character] | The one-letter type code of the organization:         - C communication cost         - D delegate         - E electioneering communication         - H House         - I independent expenditor (person or group)         - N PAC - nonqualified         - O independent expenditure-only (super PACs)         - P presidential         - Q PAC - qualified         - S Senate         - U single candidate independent expenditure         - V PAC with non-contribution account, nonqualified         - W PAC with non-contribution account, qualified         - X party, nonqualified         - Y party, qualified         - Z national party non-federal account 
#' var.min.amount <- 'min.amount_example' # character | Filter for all amounts greater than a value.
#' var.last.index <- 56 # integer | Index of last result from previous page
#' var.disbursement.description <- ['disbursement.description_example'] # array[character] | Description of disbursement
#' var.disbursement.purpose.category <- ['disbursement.purpose.category_example'] # array[character] | Disbursement purpose category
#' var.min.image.number <- 'min.image.number_example' # character | 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.last.disbursement.amount <- 3.4 # numeric | When sorting by `disbursement_amount`, this is populated with the `disbursement_amount` of the last result.  However, you will need to pass the index of that last result to `last_index` to get the next page.
#' var.max.date <- 'max.date_example' # character | Maximum date
#' var.image.number <- ['image.number_example'] # array[character] | The image number of the page where the schedule item is reported
#' var.recipient.committee.id <- ['recipient.committee.id_example'] # array[character] | The FEC identifier should be represented here if the contributor is registered with the FEC.
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.spender.committee.designation <- ['spender.committee.designation_example'] # array[character] | The one-letter designation code of the organization:          - A authorized by a candidate          - J joint fundraising committee          - P principal campaign committee of a candidate          - U unauthorized          - B lobbyist/registrant PAC          - D leadership PAC 
#' var.two.year.transaction.period <- [56] # array[integer] |  This is a two-year period that is derived from the year a transaction took place in the Itemized Schedule A and Schedule B tables. In cases where we have the date of the transaction (contribution_receipt_date in schedules/schedule_a, disbursement_date in schedules/schedule_b) the two_year_transaction_period is named after the ending, even-numbered year. If we do not have the date  of the transaction, we fall back to using the report year (report_year in both tables) instead,  making the same cycle adjustment as necessary. If no transaction year is specified, the results default to the most current cycle. 
#' var.spender.committee.org.type <- ['spender.committee.org.type_example'] # array[character] | The one-letter code for the kind for organization:         - C corporation         - L labor organization         - M membership organization         - T trade association         - V cooperative         - W corporation without capital stock 
#'
#' api.instance <- DisbursementsApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$SchedulesScheduleBGet(var.api.key, last.disbursement.date=var.last.disbursement.date, max.image.number=var.max.image.number, min.date=var.min.date, max.amount=var.max.amount, per.page=var.per.page, line.number=var.line.number, recipient.state=var.recipient.state, recipient.name=var.recipient.name, committee.id=var.committee.id, sort=var.sort, recipient.city=var.recipient.city, spender.committee.type=var.spender.committee.type, min.amount=var.min.amount, last.index=var.last.index, disbursement.description=var.disbursement.description, disbursement.purpose.category=var.disbursement.purpose.category, min.image.number=var.min.image.number, sort.hide.null=var.sort.hide.null, last.disbursement.amount=var.last.disbursement.amount, max.date=var.max.date, image.number=var.image.number, recipient.committee.id=var.recipient.committee.id, sort.null.only=var.sort.null.only, spender.committee.designation=var.spender.committee.designation, two.year.transaction.period=var.two.year.transaction.period, spender.committee.org.type=var.spender.committee.org.type)
#'
#'
#' ####################  SchedulesScheduleBSubIdGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.sub.id <- 'sub.id_example' # character | 
#' var.last.disbursement.date <- 'last.disbursement.date_example' # character | When sorting by `disbursement_date`, this is populated with the `disbursement_date` of the last result. However, you will need to pass the index of that last result to `last_index` to get the next page.
#' var.max.image.number <- 'max.image.number_example' # character | 
#' var.min.date <- 'min.date_example' # character | Minimum date
#' var.max.amount <- 'max.amount_example' # character | Filter for all amounts less than a value.
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.line.number <- 'line.number_example' # character | Filter for form and line number using the following format: `FORM-LINENUMBER`.  For example an argument such as `F3X-16` would filter down to all entries from form `F3X` line number `16`.
#' var.recipient.state <- ['recipient.state_example'] # array[character] | State of recipient
#' var.recipient.name <- ['recipient.name_example'] # array[character] | Name of the entity receiving the disbursement
#' var.committee.id <- ['committee.id_example'] # array[character] |  A unique identifier assigned to each committee or filer registered with the FEC. In general committee id's begin with the letter C which is followed by eight digits. 
#' var.sort <- '-disbursement_date' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.recipient.city <- ['recipient.city_example'] # array[character] | City of recipient
#' var.spender.committee.type <- ['spender.committee.type_example'] # array[character] | The one-letter type code of the organization:         - C communication cost         - D delegate         - E electioneering communication         - H House         - I independent expenditor (person or group)         - N PAC - nonqualified         - O independent expenditure-only (super PACs)         - P presidential         - Q PAC - qualified         - S Senate         - U single candidate independent expenditure         - V PAC with non-contribution account, nonqualified         - W PAC with non-contribution account, qualified         - X party, nonqualified         - Y party, qualified         - Z national party non-federal account 
#' var.min.amount <- 'min.amount_example' # character | Filter for all amounts greater than a value.
#' var.last.index <- 56 # integer | Index of last result from previous page
#' var.disbursement.description <- ['disbursement.description_example'] # array[character] | Description of disbursement
#' var.disbursement.purpose.category <- ['disbursement.purpose.category_example'] # array[character] | Disbursement purpose category
#' var.min.image.number <- 'min.image.number_example' # character | 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.last.disbursement.amount <- 3.4 # numeric | When sorting by `disbursement_amount`, this is populated with the `disbursement_amount` of the last result.  However, you will need to pass the index of that last result to `last_index` to get the next page.
#' var.max.date <- 'max.date_example' # character | Maximum date
#' var.image.number <- ['image.number_example'] # array[character] | The image number of the page where the schedule item is reported
#' var.recipient.committee.id <- ['recipient.committee.id_example'] # array[character] | The FEC identifier should be represented here if the contributor is registered with the FEC.
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.spender.committee.designation <- ['spender.committee.designation_example'] # array[character] | The one-letter designation code of the organization:          - A authorized by a candidate          - J joint fundraising committee          - P principal campaign committee of a candidate          - U unauthorized          - B lobbyist/registrant PAC          - D leadership PAC 
#' var.two.year.transaction.period <- [56] # array[integer] |  This is a two-year period that is derived from the year a transaction took place in the Itemized Schedule A and Schedule B tables. In cases where we have the date of the transaction (contribution_receipt_date in schedules/schedule_a, disbursement_date in schedules/schedule_b) the two_year_transaction_period is named after the ending, even-numbered year. If we do not have the date  of the transaction, we fall back to using the report year (report_year in both tables) instead,  making the same cycle adjustment as necessary. If no transaction year is specified, the results default to the most current cycle. 
#' var.spender.committee.org.type <- ['spender.committee.org.type_example'] # array[character] | The one-letter code for the kind for organization:         - C corporation         - L labor organization         - M membership organization         - T trade association         - V cooperative         - W corporation without capital stock 
#'
#' api.instance <- DisbursementsApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$SchedulesScheduleBSubIdGet(var.api.key, var.sub.id, last.disbursement.date=var.last.disbursement.date, max.image.number=var.max.image.number, min.date=var.min.date, max.amount=var.max.amount, per.page=var.per.page, line.number=var.line.number, recipient.state=var.recipient.state, recipient.name=var.recipient.name, committee.id=var.committee.id, sort=var.sort, recipient.city=var.recipient.city, spender.committee.type=var.spender.committee.type, min.amount=var.min.amount, last.index=var.last.index, disbursement.description=var.disbursement.description, disbursement.purpose.category=var.disbursement.purpose.category, min.image.number=var.min.image.number, sort.hide.null=var.sort.hide.null, last.disbursement.amount=var.last.disbursement.amount, max.date=var.max.date, image.number=var.image.number, recipient.committee.id=var.recipient.committee.id, sort.null.only=var.sort.null.only, spender.committee.designation=var.spender.committee.designation, two.year.transaction.period=var.two.year.transaction.period, spender.committee.org.type=var.spender.committee.org.type)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
DisbursementsApi <- R6::R6Class(
  'DisbursementsApi',
  public = list(
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    SchedulesScheduleBByPurposeGet = function(api.key, sort.hide.null=FALSE, purpose=NULL, per.page=20, sort.nulls.last=FALSE, committee.id=NULL, sort.null.only=FALSE, sort='null', cycle=NULL, page=1, ...){
      apiResponse <- self$SchedulesScheduleBByPurposeGetWithHttpInfo(api.key, sort.hide.null, purpose, per.page, sort.nulls.last, committee.id, sort.null.only, sort, cycle, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    SchedulesScheduleBByPurposeGetWithHttpInfo = function(api.key, sort.hide.null=FALSE, purpose=NULL, per.page=20, sort.nulls.last=FALSE, committee.id=NULL, sort.null.only=FALSE, sort='null', cycle=NULL, page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['purpose'] <- purpose

      queryParams['per_page'] <- per.page

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['committee_id'] <- committee.id

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['sort'] <- sort

      queryParams['cycle'] <- cycle

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/schedules/schedule_b/by_purpose/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "ScheduleBByPurposePage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    SchedulesScheduleBByRecipientGet = function(api.key, sort.hide.null=FALSE, per.page=20, sort.nulls.last=FALSE, recipient.name=NULL, committee.id=NULL, sort.null.only=FALSE, sort='null', cycle=NULL, page=1, ...){
      apiResponse <- self$SchedulesScheduleBByRecipientGetWithHttpInfo(api.key, sort.hide.null, per.page, sort.nulls.last, recipient.name, committee.id, sort.null.only, sort, cycle, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    SchedulesScheduleBByRecipientGetWithHttpInfo = function(api.key, sort.hide.null=FALSE, per.page=20, sort.nulls.last=FALSE, recipient.name=NULL, committee.id=NULL, sort.null.only=FALSE, sort='null', cycle=NULL, page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['per_page'] <- per.page

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['recipient_name'] <- recipient.name

      queryParams['committee_id'] <- committee.id

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['sort'] <- sort

      queryParams['cycle'] <- cycle

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/schedules/schedule_b/by_recipient/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "ScheduleBByRecipientPage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    SchedulesScheduleBByRecipientIdGet = function(api.key, sort.hide.null=FALSE, per.page=20, sort.nulls.last=FALSE, committee.id=NULL, recipient.id=NULL, sort='null', sort.null.only=FALSE, cycle=NULL, page=1, ...){
      apiResponse <- self$SchedulesScheduleBByRecipientIdGetWithHttpInfo(api.key, sort.hide.null, per.page, sort.nulls.last, committee.id, recipient.id, sort, sort.null.only, cycle, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    SchedulesScheduleBByRecipientIdGetWithHttpInfo = function(api.key, sort.hide.null=FALSE, per.page=20, sort.nulls.last=FALSE, committee.id=NULL, recipient.id=NULL, sort='null', sort.null.only=FALSE, cycle=NULL, page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['per_page'] <- per.page

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['committee_id'] <- committee.id

      queryParams['recipient_id'] <- recipient.id

      queryParams['sort'] <- sort

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['cycle'] <- cycle

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/schedules/schedule_b/by_recipient_id/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "ScheduleBByRecipientIDPage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    SchedulesScheduleBEfileGet = function(api.key, min.date=NULL, max.amount=NULL, sort.hide.null=FALSE, max.date=NULL, per.page=20, image.number=NULL, recipient.state=NULL, sort.nulls.last=FALSE, committee.id=NULL, sort.null.only=FALSE, sort='-disbursement_date', page=1, recipient.city=NULL, min.amount=NULL, disbursement.description=NULL, ...){
      apiResponse <- self$SchedulesScheduleBEfileGetWithHttpInfo(api.key, min.date, max.amount, sort.hide.null, max.date, per.page, image.number, recipient.state, sort.nulls.last, committee.id, sort.null.only, sort, page, recipient.city, min.amount, disbursement.description, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    SchedulesScheduleBEfileGetWithHttpInfo = function(api.key, min.date=NULL, max.amount=NULL, sort.hide.null=FALSE, max.date=NULL, per.page=20, image.number=NULL, recipient.state=NULL, sort.nulls.last=FALSE, committee.id=NULL, sort.null.only=FALSE, sort='-disbursement_date', page=1, recipient.city=NULL, min.amount=NULL, disbursement.description=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['min_date'] <- min.date

      queryParams['max_amount'] <- max.amount

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['max_date'] <- max.date

      queryParams['per_page'] <- per.page

      queryParams['image_number'] <- image.number

      queryParams['recipient_state'] <- recipient.state

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['committee_id'] <- committee.id

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['sort'] <- sort

      queryParams['page'] <- page

      queryParams['recipient_city'] <- recipient.city

      queryParams['min_amount'] <- min.amount

      queryParams['disbursement_description'] <- disbursement.description

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/schedules/schedule_b/efile/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "ScheduleBEfilePage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    SchedulesScheduleBGet = function(api.key, last.disbursement.date=NULL, max.image.number=NULL, min.date=NULL, max.amount=NULL, per.page=20, line.number=NULL, recipient.state=NULL, recipient.name=NULL, committee.id=NULL, sort='-disbursement_date', recipient.city=NULL, spender.committee.type=NULL, min.amount=NULL, last.index=NULL, disbursement.description=NULL, disbursement.purpose.category=NULL, min.image.number=NULL, sort.hide.null=FALSE, last.disbursement.amount=NULL, max.date=NULL, image.number=NULL, recipient.committee.id=NULL, sort.null.only=FALSE, spender.committee.designation=NULL, two.year.transaction.period=NULL, spender.committee.org.type=NULL, ...){
      apiResponse <- self$SchedulesScheduleBGetWithHttpInfo(api.key, last.disbursement.date, max.image.number, min.date, max.amount, per.page, line.number, recipient.state, recipient.name, committee.id, sort, recipient.city, spender.committee.type, min.amount, last.index, disbursement.description, disbursement.purpose.category, min.image.number, sort.hide.null, last.disbursement.amount, max.date, image.number, recipient.committee.id, sort.null.only, spender.committee.designation, two.year.transaction.period, spender.committee.org.type, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    SchedulesScheduleBGetWithHttpInfo = function(api.key, last.disbursement.date=NULL, max.image.number=NULL, min.date=NULL, max.amount=NULL, per.page=20, line.number=NULL, recipient.state=NULL, recipient.name=NULL, committee.id=NULL, sort='-disbursement_date', recipient.city=NULL, spender.committee.type=NULL, min.amount=NULL, last.index=NULL, disbursement.description=NULL, disbursement.purpose.category=NULL, min.image.number=NULL, sort.hide.null=FALSE, last.disbursement.amount=NULL, max.date=NULL, image.number=NULL, recipient.committee.id=NULL, sort.null.only=FALSE, spender.committee.designation=NULL, two.year.transaction.period=NULL, spender.committee.org.type=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['last_disbursement_date'] <- last.disbursement.date

      queryParams['max_image_number'] <- max.image.number

      queryParams['min_date'] <- min.date

      queryParams['max_amount'] <- max.amount

      queryParams['per_page'] <- per.page

      queryParams['line_number'] <- line.number

      queryParams['recipient_state'] <- recipient.state

      queryParams['recipient_name'] <- recipient.name

      queryParams['committee_id'] <- committee.id

      queryParams['sort'] <- sort

      queryParams['recipient_city'] <- recipient.city

      queryParams['spender_committee_type'] <- spender.committee.type

      queryParams['min_amount'] <- min.amount

      queryParams['last_index'] <- last.index

      queryParams['disbursement_description'] <- disbursement.description

      queryParams['disbursement_purpose_category'] <- disbursement.purpose.category

      queryParams['min_image_number'] <- min.image.number

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['last_disbursement_amount'] <- last.disbursement.amount

      queryParams['max_date'] <- max.date

      queryParams['image_number'] <- image.number

      queryParams['recipient_committee_id'] <- recipient.committee.id

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['spender_committee_designation'] <- spender.committee.designation

      queryParams['two_year_transaction_period'] <- two.year.transaction.period

      queryParams['spender_committee_org_type'] <- spender.committee.org.type

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/schedules/schedule_b/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "ScheduleBPage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    SchedulesScheduleBSubIdGet = function(api.key, sub.id, last.disbursement.date=NULL, max.image.number=NULL, min.date=NULL, max.amount=NULL, per.page=20, line.number=NULL, recipient.state=NULL, recipient.name=NULL, committee.id=NULL, sort='-disbursement_date', recipient.city=NULL, spender.committee.type=NULL, min.amount=NULL, last.index=NULL, disbursement.description=NULL, disbursement.purpose.category=NULL, min.image.number=NULL, sort.hide.null=FALSE, last.disbursement.amount=NULL, max.date=NULL, image.number=NULL, recipient.committee.id=NULL, sort.null.only=FALSE, spender.committee.designation=NULL, two.year.transaction.period=NULL, spender.committee.org.type=NULL, ...){
      apiResponse <- self$SchedulesScheduleBSubIdGetWithHttpInfo(api.key, sub.id, last.disbursement.date, max.image.number, min.date, max.amount, per.page, line.number, recipient.state, recipient.name, committee.id, sort, recipient.city, spender.committee.type, min.amount, last.index, disbursement.description, disbursement.purpose.category, min.image.number, sort.hide.null, last.disbursement.amount, max.date, image.number, recipient.committee.id, sort.null.only, spender.committee.designation, two.year.transaction.period, spender.committee.org.type, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    SchedulesScheduleBSubIdGetWithHttpInfo = function(api.key, sub.id, last.disbursement.date=NULL, max.image.number=NULL, min.date=NULL, max.amount=NULL, per.page=20, line.number=NULL, recipient.state=NULL, recipient.name=NULL, committee.id=NULL, sort='-disbursement_date', recipient.city=NULL, spender.committee.type=NULL, min.amount=NULL, last.index=NULL, disbursement.description=NULL, disbursement.purpose.category=NULL, min.image.number=NULL, sort.hide.null=FALSE, last.disbursement.amount=NULL, max.date=NULL, image.number=NULL, recipient.committee.id=NULL, sort.null.only=FALSE, spender.committee.designation=NULL, two.year.transaction.period=NULL, spender.committee.org.type=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      if (missing(`sub.id`)) {
        stop("Missing required parameter `sub.id`.")
      }

      queryParams['last_disbursement_date'] <- last.disbursement.date

      queryParams['max_image_number'] <- max.image.number

      queryParams['min_date'] <- min.date

      queryParams['max_amount'] <- max.amount

      queryParams['per_page'] <- per.page

      queryParams['line_number'] <- line.number

      queryParams['recipient_state'] <- recipient.state

      queryParams['recipient_name'] <- recipient.name

      queryParams['committee_id'] <- committee.id

      queryParams['sort'] <- sort

      queryParams['recipient_city'] <- recipient.city

      queryParams['spender_committee_type'] <- spender.committee.type

      queryParams['min_amount'] <- min.amount

      queryParams['last_index'] <- last.index

      queryParams['disbursement_description'] <- disbursement.description

      queryParams['disbursement_purpose_category'] <- disbursement.purpose.category

      queryParams['min_image_number'] <- min.image.number

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['last_disbursement_amount'] <- last.disbursement.amount

      queryParams['max_date'] <- max.date

      queryParams['image_number'] <- image.number

      queryParams['recipient_committee_id'] <- recipient.committee.id

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['spender_committee_designation'] <- spender.committee.designation

      queryParams['two_year_transaction_period'] <- two.year.transaction.period

      queryParams['spender_committee_org_type'] <- spender.committee.org.type

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/schedules/schedule_b/{sub_id}/"
      if (!missing(`sub.id`)) {
        urlPath <- gsub(paste0("\\{", "sub_id", "\\}"), URLencode(as.character(`sub.id`), reserved = TRUE), urlPath)
      }

      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "ScheduleBPage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    }
  )
)
