# OpenFEC
#
# This application programming interface (API) allows you to explore the way candidates and committees fund their campaigns.    The Federal Election Commission (FEC) API is a RESTful web service supporting full-text and field-specific searches on FEC data. [Bulk downloads](https://www.fec.gov/data/advanced/?tab=bulk-data) are available on the current site. Information is tied to the underlying forms by file ID and image ID. Data is updated nightly.    There are a lot of data, and a good place to start is to use search to find interesting candidates and committees. Then, you can use their IDs to find report or line item details with the other endpoints. If you are interested in individual donors, check out contributor information in the `/schedule_a/` endpoints.    <b class=\"body\" id=\"getting_started_head\">Getting started with the openFEC API</b><br>    If you would like to use the FEC's API programmatically, you can sign up for your own API key using our form. Alternatively, you can still try out our API without an API key by using the web interface and using DEMO_KEY. Note that when you use the openFEC API you are subject to the [Terms of Service](https://github.com/fecgov/FEC/blob/master/TERMS-OF-SERVICE.md) and [Acceptable Use policy](https://github.com/fecgov/FEC/blob/master/ACCEPTABLE-USE-POLICY.md).    Signing up for an API key will enable you to place up to 1,000 calls an hour. Each call is limited to 100 results per page. You can email questions, comments or a request to get a key for 7,200 calls an hour (120 calls per minute) to <a href=\"mailto:APIinfo@fec.gov\">APIinfo@fec.gov</a>. You can also ask questions and discuss the data in a community led [group](https://groups.google.com/forum/#!forum/fec-data).    The model definitions and schema are available at [/swagger](/swagger/). This is useful for making wrappers and exploring the data.    A few restrictions limit the way you can use FEC data. For example, you can’t use contributor lists for commercial purposes or to solicit donations. [Learn more here](https://www.fec.gov/updates/sale-or-use-contributor-information/).    [Inspect our source code](https://github.com/fecgov/openFEC). We welcome issues and pull requests!    <p><br></p> <h2 class=\"title\" id=\"signup_head\">Sign up for an API key</h2> <div id=\"apidatagov_signup\">Loading signup form...</div>
#
# The version of the OpenAPI document: 1.0
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title IndependentExpenditures operations
#' @description openapi.IndependentExpenditures
#' @format An \code{R6Class} generator object
#' @field apiClient Handles the client-server communication.
#'
#' @section Methods:
#' \describe{
#' \strong{ SchedulesScheduleEByCandidateGet } \emph{  }
#'  Schedule E receipts aggregated by recipient candidate. To avoid double counting, memoed items are not included. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } election.full character
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } district character
#' \item \emph{ @param } candidate.id list( character )
#' \item \emph{ @param } committee.id list( character )
#' \item \emph{ @param } sort character
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } office Enum < [house, senate, president] > 
#' \item \emph{ @param } state character
#' \item \emph{ @param } cycle list( integer )
#' \item \emph{ @param } support.oppose Enum < [S, O] > 
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{ScheduleEByCandidatePage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : ScheduleEByCandidatePage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SchedulesScheduleEEfileGet } \emph{  }
#'  Efiling endpoints provide real-time campaign finance data received from electronic filers. Efiling endpoints only contain the most recent four months of data and don&#39;t contain the processed and coded data that you can find on other endpoints. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } support.oppose.indicator Enum < [S, O] > 
#' \item \emph{ @param } min.expenditure.amount integer
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } is.notice character
#' \item \emph{ @param } payee.name list( character )
#' \item \emph{ @param } min.dissemination.date character
#' \item \emph{ @param } committee.id list( character )
#' \item \emph{ @param } min.expenditure.date character
#' \item \emph{ @param } max.expenditure.date character
#' \item \emph{ @param } sort character
#' \item \emph{ @param } min.filed.date character
#' \item \emph{ @param } candidate.office.state list( character )
#' \item \emph{ @param } max.dissemination.date character
#' \item \emph{ @param } max.expenditure.amount integer
#' \item \emph{ @param } candidate.party list( character )
#' \item \emph{ @param } max.filed.date character
#' \item \emph{ @param } candidate.search list( character )
#' \item \emph{ @param } filing.form list( character )
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } candidate.office Enum < [, H, S, P] > 
#' \item \emph{ @param } image.number list( character )
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } spender.name list( character )
#' \item \emph{ @param } candidate.id list( character )
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } most.recent character
#' \item \emph{ @param } candidate.office.district list( character )
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{ScheduleEEfilePage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : ScheduleEEfilePage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SchedulesScheduleEGet } \emph{  }
#'  Schedule E covers the line item expenditures for independent expenditures. For example, if a super PAC bought ads on TV to oppose a federal candidate, each ad purchase would be recorded here with the expenditure amount, name and id of the candidate, and whether the ad supported or opposed the candidate.  An independent expenditure is an expenditure for a communication \&quot;expressly advocating the election or defeat of a clearly identified candidate that is not made in cooperation, consultation, or concert with, or at the request or suggestion of, a candidate, a candidate’s authorized committee, or their agents, or a political party or its agents.\&quot;  Aggregates by candidate do not include 24 and 48 hour reports. This ensures we don&#39;t double count expenditures and the totals are more accurate. You can still find the information from 24 and 48 hour reports in &#x60;/schedule/schedule_e/&#x60;.  Due to the large quantity of Schedule E filings, this endpoint is not paginated by page number. Instead, you can request the next page of results by adding the values in the &#x60;last_indexes&#x60; object from &#x60;pagination&#x60; to the URL of your last request. For example, when sorting by &#x60;expenditure_amount&#x60;, you might receive a page of results with the following pagination information:  &#x60;&#x60;&#x60;  \&quot;pagination\&quot;: {     \&quot;count\&quot;: 152623,     \&quot;last_indexes\&quot;: {       \&quot;last_index\&quot;: \&quot;3023037\&quot;,       \&quot;last_expenditure_amount\&quot;: -17348.5     },     \&quot;per_page\&quot;: 20,     \&quot;pages\&quot;: 7632   } } &#x60;&#x60;&#x60;  To fetch the next page of sorted results, append &#x60;last_index&#x3D;3023037&#x60; and &#x60;last_expenditure_amount&#x3D;&#x60; to the URL.  We strongly advise paging through these results by using the sort indices (defaults to sort by disbursement date, e.g. &#x60;last_disbursement_date&#x60;), otherwise some resources may be unintentionally filtered out.  This resource uses keyset pagination to improve query performance and these indices are required to properly page through this large dataset.  Note: because the Schedule E data includes many records, counts for large result sets are approximate; you will want to page through the records until no records are returned. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } support.oppose.indicator Enum < [S, O] > 
#' \item \emph{ @param } max.image.number character
#' \item \emph{ @param } max.filing.date character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } is.notice list( character )
#' \item \emph{ @param } min.dissemination.date character
#' \item \emph{ @param } committee.id list( character )
#' \item \emph{ @param } sort character
#' \item \emph{ @param } candidate.party list( character )
#' \item \emph{ @param } last.expenditure.amount numeric
#' \item \emph{ @param } min.amount character
#' \item \emph{ @param } last.index integer
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } max.date character
#' \item \emph{ @param } image.number list( character )
#' \item \emph{ @param } most.recent character
#' \item \emph{ @param } candidate.office.district list( character )
#' \item \emph{ @param } min.date character
#' \item \emph{ @param } max.amount character
#' \item \emph{ @param } line.number character
#' \item \emph{ @param } payee.name list( character )
#' \item \emph{ @param } last.support.oppose.indicator character
#' \item \emph{ @param } candidate.office.state list( character )
#' \item \emph{ @param } max.dissemination.date character
#' \item \emph{ @param } cycle list( integer )
#' \item \emph{ @param } last.office.total.ytd numeric
#' \item \emph{ @param } filing.form list( character )
#' \item \emph{ @param } min.image.number character
#' \item \emph{ @param } candidate.office Enum < [, H, S, P] > 
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } candidate.id list( character )
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } min.filing.date character
#' \item \emph{ @param } last.expenditure.date character
#' \item \emph{ @returnType } \link{ScheduleEPage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : ScheduleEPage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' \strong{ SchedulesScheduleETotalsByCandidateGet } \emph{  }
#'  Total independent expenditure on supported or opposed candidates by cycle or candidate election year. 
#'
#' \itemize{
#' \item \emph{ @param } api.key character
#' \item \emph{ @param } sort.hide.null character
#' \item \emph{ @param } per.page integer
#' \item \emph{ @param } election.full character
#' \item \emph{ @param } sort.nulls.last character
#' \item \emph{ @param } candidate.id list( character )
#' \item \emph{ @param } sort.null.only character
#' \item \emph{ @param } sort character
#' \item \emph{ @param } cycle list( integer )
#' \item \emph{ @param } page integer
#' \item \emph{ @returnType } \link{IETotalsByCandidatePage} \cr
#'
#'
#' \item status code : 0 | 
#'
#' \item return type : IETotalsByCandidatePage 
#' \item response headers :
#'
#' \tabular{ll}{
#' }
#' }
#'
#' }
#'
#'
#' @examples
#' \dontrun{
#' ####################  SchedulesScheduleEByCandidateGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.election.full <- TRUE # character | `True` indicates that full election period of a candidate. `False` indicates that two year election cycle.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.district <- 'district_example' # character | Two-digit US House distirict of the office the candidate is running for. Presidential, Senate and House at-large candidates will have District 00.
#' var.candidate.id <- ['candidate.id_example'] # array[character] |  A unique identifier assigned to each candidate registered with the FEC. If a person runs for several offices, that person will have separate candidate IDs for each office. 
#' var.committee.id <- ['committee.id_example'] # array[character] |  A unique identifier assigned to each committee or filer registered with the FEC. In general committee id's begin with the letter C which is followed by eight digits. 
#' var.sort <- 'null' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.office <- 'office_example' # character | Federal office candidate runs for: H, S or P
#' var.state <- 'state_example' # character | US state or territory where a candidate runs for office
#' var.cycle <- [56] # array[integer] |  Filter records to only those that were applicable to a given two-year period.The cycle begins with an odd year and is named for its ending, even year. 
#' var.support.oppose <- 'null' # character | Support or opposition
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- IndependentExpendituresApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$SchedulesScheduleEByCandidateGet(var.api.key, sort.hide.null=var.sort.hide.null, per.page=var.per.page, election.full=var.election.full, sort.nulls.last=var.sort.nulls.last, district=var.district, candidate.id=var.candidate.id, committee.id=var.committee.id, sort=var.sort, sort.null.only=var.sort.null.only, office=var.office, state=var.state, cycle=var.cycle, support.oppose=var.support.oppose, page=var.page)
#'
#'
#' ####################  SchedulesScheduleEEfileGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.support.oppose.indicator <- ['support.oppose.indicator_example'] # array[character] | Explains if the money was spent in order to support or oppose a candidate or candidates. (Coded S or O for support or oppose.) This indicator applies to independent expenditures and communication costs.
#' var.min.expenditure.amount <- 56 # integer | Selects all items expended by this committee greater than this amount
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.is.notice <- 'is.notice_example' # character |  Record filed as 24- or 48-hour notice. 
#' var.payee.name <- ['payee.name_example'] # array[character] |  Name of the entity that received the payment. 
#' var.min.dissemination.date <- 'min.dissemination.date_example' # character | Selects all items distributed by this committee after this date
#' var.committee.id <- ['committee.id_example'] # array[character] |  A unique identifier assigned to each committee or filer registered with the FEC. In general committee id's begin with the letter C which is followed by eight digits. 
#' var.min.expenditure.date <- 'min.expenditure.date_example' # character | Selects all items expended by this committee after this date
#' var.max.expenditure.date <- 'max.expenditure.date_example' # character | Selects all items expended by this committee before this date
#' var.sort <- '-expenditure_date' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.min.filed.date <- 'min.filed.date_example' # character | Timestamp of electronic or paper record that FEC received
#' var.candidate.office.state <- ['candidate.office.state_example'] # array[character] | US state or territory where a candidate runs for office
#' var.max.dissemination.date <- 'max.dissemination.date_example' # character | Selects all items distributed by this committee before this date
#' var.max.expenditure.amount <- 56 # integer | Selects all items expended by this committee less than this amount
#' var.candidate.party <- ['candidate.party_example'] # array[character] | Three-letter code for the party affiliated with a candidate or committee. For example, DEM for Democratic Party and REP for Republican Party.
#' var.max.filed.date <- 'max.filed.date_example' # character | Timestamp of electronic or paper record that FEC received
#' var.candidate.search <- ['candidate.search_example'] # array[character] |  Search for candidates by candiate id or candidate first or last name 
#' var.filing.form <- ['filing.form_example'] # array[character] | The form where the underlying data comes from, for example, Form 1 would appear as F1:     - F1   Statement of Organization     - F1M  Notification of Multicandidate Status     - F2   Statement of Candidacy     - F3   Report of Receipts and Disbursements for an Authorized Committee     - F3P  Report of Receipts and Disbursements by an Authorized Committee of a Candidate for     The Office of President or Vice President     - F3L  Report of Contributions Bundled by Lobbyists/Registrants and Lobbyist/Registrant PACs     - F3X  Report of Receipts and Disbursements for other than an Authorized Committee     - F4   Report of Receipts and Disbursements for a Committee or Organization Supporting a Nomination Convention     - F5   Report of Independent Expenditures Made and Contributions Received     - F6   48 Hour Notice of Contributions/Loans Received     - F7   Report of Communication Costs by Corporations and Membership Organizations     - F8   Debt Settlement Plan     - F9   24 Hour Notice of Disbursements for Electioneering Communications     - F13  Report of Donations Accepted for Inaugural Committee     - F99  Miscellaneous Text     - FRQ  Request for Additional Information 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.candidate.office <- 'candidate.office_example' # character | Federal office candidate runs for: H, S or P
#' var.image.number <- ['image.number_example'] # array[character] |  An unique identifier for each page where the electronic or paper filing is reported. 
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.spender.name <- ['spender.name_example'] # array[character] | The name of the committee. If a committee changes its name,     the most recent name will be shown. Committee names are not unique. Use committee_id     for looking up records.
#' var.candidate.id <- ['candidate.id_example'] # array[character] |  A unique identifier assigned to each candidate registered with the FEC. If a person runs for several offices, that person will have separate candidate IDs for each office. 
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.most.recent <- 'most.recent_example' # character |  The report associated with the transaction is either new or is the most-recently filed amendment. Undetermined version (`null`) is always included. 
#' var.candidate.office.district <- ['candidate.office.district_example'] # array[character] | Two-digit US House distirict of the office the candidate is running for. Presidential, Senate and House at-large candidates will have District 00.
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- IndependentExpendituresApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$SchedulesScheduleEEfileGet(var.api.key, support.oppose.indicator=var.support.oppose.indicator, min.expenditure.amount=var.min.expenditure.amount, per.page=var.per.page, is.notice=var.is.notice, payee.name=var.payee.name, min.dissemination.date=var.min.dissemination.date, committee.id=var.committee.id, min.expenditure.date=var.min.expenditure.date, max.expenditure.date=var.max.expenditure.date, sort=var.sort, min.filed.date=var.min.filed.date, candidate.office.state=var.candidate.office.state, max.dissemination.date=var.max.dissemination.date, max.expenditure.amount=var.max.expenditure.amount, candidate.party=var.candidate.party, max.filed.date=var.max.filed.date, candidate.search=var.candidate.search, filing.form=var.filing.form, sort.hide.null=var.sort.hide.null, candidate.office=var.candidate.office, image.number=var.image.number, sort.nulls.last=var.sort.nulls.last, spender.name=var.spender.name, candidate.id=var.candidate.id, sort.null.only=var.sort.null.only, most.recent=var.most.recent, candidate.office.district=var.candidate.office.district, page=var.page)
#'
#'
#' ####################  SchedulesScheduleEGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.support.oppose.indicator <- ['support.oppose.indicator_example'] # array[character] | Explains if the money was spent in order to support or oppose a candidate or candidates. (Coded S or O for support or oppose.) This indicator applies to independent expenditures and communication costs.
#' var.max.image.number <- 'max.image.number_example' # character | 
#' var.max.filing.date <- 'max.filing.date_example' # character |  Selects all filings received before this date 
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.is.notice <- ['is.notice_example'] # array[character] |  Record filed as 24- or 48-hour notice. 
#' var.min.dissemination.date <- 'min.dissemination.date_example' # character | Selects all items distributed by this committee after this date
#' var.committee.id <- ['committee.id_example'] # array[character] |  A unique identifier assigned to each committee or filer registered with the FEC. In general committee id's begin with the letter C which is followed by eight digits. 
#' var.sort <- '-expenditure_date' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.candidate.party <- ['candidate.party_example'] # array[character] | Three-letter code for the party affiliated with a candidate or committee. For example, DEM for Democratic Party and REP for Republican Party.
#' var.last.expenditure.amount <- 3.4 # numeric |  When sorting by `expenditure_amount`, this is populated with the `expenditure_amount` of the last result. However, you will need to pass the index of that last result to `last_index` to get the next page. 
#' var.min.amount <- 'min.amount_example' # character | Filter for all amounts greater than a value.
#' var.last.index <- 56 # integer | Index of last result from previous page
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.max.date <- 'max.date_example' # character | Maximum date
#' var.image.number <- ['image.number_example'] # array[character] | The image number of the page where the schedule item is reported
#' var.most.recent <- 'most.recent_example' # character |  The report associated with the transaction is either new or is the most-recently filed amendment. Undetermined version (`null`) is always included. 
#' var.candidate.office.district <- ['candidate.office.district_example'] # array[character] | Two-digit US House distirict of the office the candidate is running for. Presidential, Senate and House at-large candidates will have District 00.
#' var.min.date <- 'min.date_example' # character | Minimum date
#' var.max.amount <- 'max.amount_example' # character | Filter for all amounts less than a value.
#' var.line.number <- 'line.number_example' # character | Filter for form and line number using the following format: `FORM-LINENUMBER`.  For example an argument such as `F3X-16` would filter down to all entries from form `F3X` line number `16`.
#' var.payee.name <- ['payee.name_example'] # array[character] |  Name of the entity that received the payment. 
#' var.last.support.oppose.indicator <- 'null' # character |  When sorting by `support_oppose_indicator`, this is populated with the `support_oppose_indicator` of the last result. However, you will need to pass the index of that last result to `last_index` to get the next page.' 
#' var.candidate.office.state <- ['candidate.office.state_example'] # array[character] | US state or territory
#' var.max.dissemination.date <- 'max.dissemination.date_example' # character | Selects all items distributed by this committee before this date
#' var.cycle <- [56] # array[integer] |  Filter records to only those that were applicable to a given two-year period.The cycle begins with an odd year and is named for its ending, even year. 
#' var.last.office.total.ytd <- 3.4 # numeric |  When sorting by `office_total_ytd`, this is populated with the `office_total_ytd` of the last result. However, you will need to pass the index of that last result to `last_index` to get the next page.' 
#' var.filing.form <- ['filing.form_example'] # array[character] | The form where the underlying data comes from, for example, Form 1 would appear as F1:     - F1   Statement of Organization     - F1M  Notification of Multicandidate Status     - F2   Statement of Candidacy     - F3   Report of Receipts and Disbursements for an Authorized Committee     - F3P  Report of Receipts and Disbursements by an Authorized Committee of a Candidate for     The Office of President or Vice President     - F3L  Report of Contributions Bundled by Lobbyists/Registrants and Lobbyist/Registrant PACs     - F3X  Report of Receipts and Disbursements for other than an Authorized Committee     - F4   Report of Receipts and Disbursements for a Committee or Organization Supporting a Nomination Convention     - F5   Report of Independent Expenditures Made and Contributions Received     - F6   48 Hour Notice of Contributions/Loans Received     - F7   Report of Communication Costs by Corporations and Membership Organizations     - F8   Debt Settlement Plan     - F9   24 Hour Notice of Disbursements for Electioneering Communications     - F13  Report of Donations Accepted for Inaugural Committee     - F99  Miscellaneous Text     - FRQ  Request for Additional Information 
#' var.min.image.number <- 'min.image.number_example' # character | 
#' var.candidate.office <- ['candidate.office_example'] # array[character] | Federal office candidate runs for: H, S or P
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.candidate.id <- ['candidate.id_example'] # array[character] |  A unique identifier assigned to each candidate registered with the FEC. If a person runs for several offices, that person will have separate candidate IDs for each office. 
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.min.filing.date <- 'min.filing.date_example' # character |  Selects all filings received after this date 
#' var.last.expenditure.date <- 'last.expenditure.date_example' # character |  When sorting by `expenditure_date`, this is populated with the `expenditure_date` of the last result. However, you will need to pass the index of that last result to `last_index` to get the next page. 
#'
#' api.instance <- IndependentExpendituresApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$SchedulesScheduleEGet(var.api.key, support.oppose.indicator=var.support.oppose.indicator, max.image.number=var.max.image.number, max.filing.date=var.max.filing.date, per.page=var.per.page, is.notice=var.is.notice, min.dissemination.date=var.min.dissemination.date, committee.id=var.committee.id, sort=var.sort, candidate.party=var.candidate.party, last.expenditure.amount=var.last.expenditure.amount, min.amount=var.min.amount, last.index=var.last.index, sort.hide.null=var.sort.hide.null, max.date=var.max.date, image.number=var.image.number, most.recent=var.most.recent, candidate.office.district=var.candidate.office.district, min.date=var.min.date, max.amount=var.max.amount, line.number=var.line.number, payee.name=var.payee.name, last.support.oppose.indicator=var.last.support.oppose.indicator, candidate.office.state=var.candidate.office.state, max.dissemination.date=var.max.dissemination.date, cycle=var.cycle, last.office.total.ytd=var.last.office.total.ytd, filing.form=var.filing.form, min.image.number=var.min.image.number, candidate.office=var.candidate.office, sort.nulls.last=var.sort.nulls.last, candidate.id=var.candidate.id, sort.null.only=var.sort.null.only, min.filing.date=var.min.filing.date, last.expenditure.date=var.last.expenditure.date)
#'
#'
#' ####################  SchedulesScheduleETotalsByCandidateGet  ####################
#'
#' library(openapi)
#' var.api.key <- 'DEMO_KEY' # character |  API key for https://api.data.gov. Get one at https://api.data.gov/signup. 
#' var.sort.hide.null <- FALSE # character | Hide null values on sorted column(s).
#' var.per.page <- 20 # integer | The number of results returned per page. Defaults to 20.
#' var.election.full <- TRUE # character | `True` indicates that full election period of a candidate. `False` indicates that two year election cycle.
#' var.sort.nulls.last <- FALSE # character | Toggle that sorts null values last
#' var.candidate.id <- ['candidate.id_example'] # array[character] |  A unique identifier assigned to each candidate registered with the FEC. If a person runs for several offices, that person will have separate candidate IDs for each office. 
#' var.sort.null.only <- FALSE # character | Toggle that filters out all rows having sort column that is non-null
#' var.sort <- 'null' # character | Provide a field to sort by. Use `-` for descending order. 
#' var.cycle <- [56] # array[integer] |  Filter records to only those that were applicable to a given two-year period.The cycle begins with an odd year and is named for its ending, even year. 
#' var.page <- 1 # integer | For paginating through results, starting at page 1
#'
#' api.instance <- IndependentExpendituresApi$new()
#'
#' #Configure API key authorization: ApiKeyHeaderAuth
#' api.instance$apiClient$apiKeys['X-Api-Key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: ApiKeyQueryAuth
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' #Configure API key authorization: apiKey
#' api.instance$apiClient$apiKeys['api_key'] <- 'TODO_YOUR_API_KEY';
#'
#' result <- api.instance$SchedulesScheduleETotalsByCandidateGet(var.api.key, sort.hide.null=var.sort.hide.null, per.page=var.per.page, election.full=var.election.full, sort.nulls.last=var.sort.nulls.last, candidate.id=var.candidate.id, sort.null.only=var.sort.null.only, sort=var.sort, cycle=var.cycle, page=var.page)
#'
#'
#' }
#' @importFrom R6 R6Class
#' @importFrom base64enc base64encode
#' @export
IndependentExpendituresApi <- R6::R6Class(
  'IndependentExpendituresApi',
  public = list(
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    SchedulesScheduleEByCandidateGet = function(api.key, sort.hide.null=FALSE, per.page=20, election.full=TRUE, sort.nulls.last=FALSE, district=NULL, candidate.id=NULL, committee.id=NULL, sort='null', sort.null.only=FALSE, office=NULL, state=NULL, cycle=NULL, support.oppose='null', page=1, ...){
      apiResponse <- self$SchedulesScheduleEByCandidateGetWithHttpInfo(api.key, sort.hide.null, per.page, election.full, sort.nulls.last, district, candidate.id, committee.id, sort, sort.null.only, office, state, cycle, support.oppose, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    SchedulesScheduleEByCandidateGetWithHttpInfo = function(api.key, sort.hide.null=FALSE, per.page=20, election.full=TRUE, sort.nulls.last=FALSE, district=NULL, candidate.id=NULL, committee.id=NULL, sort='null', sort.null.only=FALSE, office=NULL, state=NULL, cycle=NULL, support.oppose='null', page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['per_page'] <- per.page

      queryParams['election_full'] <- election.full

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['district'] <- district

      queryParams['candidate_id'] <- candidate.id

      queryParams['committee_id'] <- committee.id

      queryParams['sort'] <- sort

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['office'] <- office

      queryParams['state'] <- state

      queryParams['cycle'] <- cycle

      queryParams['support_oppose'] <- support.oppose

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/schedules/schedule_e/by_candidate/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "ScheduleEByCandidatePage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    SchedulesScheduleEEfileGet = function(api.key, support.oppose.indicator=NULL, min.expenditure.amount=NULL, per.page=20, is.notice=NULL, payee.name=NULL, min.dissemination.date=NULL, committee.id=NULL, min.expenditure.date=NULL, max.expenditure.date=NULL, sort='-expenditure_date', min.filed.date=NULL, candidate.office.state=NULL, max.dissemination.date=NULL, max.expenditure.amount=NULL, candidate.party=NULL, max.filed.date=NULL, candidate.search=NULL, filing.form=NULL, sort.hide.null=FALSE, candidate.office=NULL, image.number=NULL, sort.nulls.last=FALSE, spender.name=NULL, candidate.id=NULL, sort.null.only=FALSE, most.recent=NULL, candidate.office.district=NULL, page=1, ...){
      apiResponse <- self$SchedulesScheduleEEfileGetWithHttpInfo(api.key, support.oppose.indicator, min.expenditure.amount, per.page, is.notice, payee.name, min.dissemination.date, committee.id, min.expenditure.date, max.expenditure.date, sort, min.filed.date, candidate.office.state, max.dissemination.date, max.expenditure.amount, candidate.party, max.filed.date, candidate.search, filing.form, sort.hide.null, candidate.office, image.number, sort.nulls.last, spender.name, candidate.id, sort.null.only, most.recent, candidate.office.district, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    SchedulesScheduleEEfileGetWithHttpInfo = function(api.key, support.oppose.indicator=NULL, min.expenditure.amount=NULL, per.page=20, is.notice=NULL, payee.name=NULL, min.dissemination.date=NULL, committee.id=NULL, min.expenditure.date=NULL, max.expenditure.date=NULL, sort='-expenditure_date', min.filed.date=NULL, candidate.office.state=NULL, max.dissemination.date=NULL, max.expenditure.amount=NULL, candidate.party=NULL, max.filed.date=NULL, candidate.search=NULL, filing.form=NULL, sort.hide.null=FALSE, candidate.office=NULL, image.number=NULL, sort.nulls.last=FALSE, spender.name=NULL, candidate.id=NULL, sort.null.only=FALSE, most.recent=NULL, candidate.office.district=NULL, page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['support_oppose_indicator'] <- support.oppose.indicator

      queryParams['min_expenditure_amount'] <- min.expenditure.amount

      queryParams['per_page'] <- per.page

      queryParams['is_notice'] <- is.notice

      queryParams['payee_name'] <- payee.name

      queryParams['min_dissemination_date'] <- min.dissemination.date

      queryParams['committee_id'] <- committee.id

      queryParams['min_expenditure_date'] <- min.expenditure.date

      queryParams['max_expenditure_date'] <- max.expenditure.date

      queryParams['sort'] <- sort

      queryParams['min_filed_date'] <- min.filed.date

      queryParams['candidate_office_state'] <- candidate.office.state

      queryParams['max_dissemination_date'] <- max.dissemination.date

      queryParams['max_expenditure_amount'] <- max.expenditure.amount

      queryParams['candidate_party'] <- candidate.party

      queryParams['max_filed_date'] <- max.filed.date

      queryParams['candidate_search'] <- candidate.search

      queryParams['filing_form'] <- filing.form

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['candidate_office'] <- candidate.office

      queryParams['image_number'] <- image.number

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['spender_name'] <- spender.name

      queryParams['candidate_id'] <- candidate.id

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['most_recent'] <- most.recent

      queryParams['candidate_office_district'] <- candidate.office.district

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/schedules/schedule_e/efile/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "ScheduleEEfilePage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    SchedulesScheduleEGet = function(api.key, support.oppose.indicator=NULL, max.image.number=NULL, max.filing.date=NULL, per.page=20, is.notice=NULL, min.dissemination.date=NULL, committee.id=NULL, sort='-expenditure_date', candidate.party=NULL, last.expenditure.amount=NULL, min.amount=NULL, last.index=NULL, sort.hide.null=FALSE, max.date=NULL, image.number=NULL, most.recent=NULL, candidate.office.district=NULL, min.date=NULL, max.amount=NULL, line.number=NULL, payee.name=NULL, last.support.oppose.indicator='null', candidate.office.state=NULL, max.dissemination.date=NULL, cycle=NULL, last.office.total.ytd=NULL, filing.form=NULL, min.image.number=NULL, candidate.office=NULL, sort.nulls.last=FALSE, candidate.id=NULL, sort.null.only=FALSE, min.filing.date=NULL, last.expenditure.date=NULL, ...){
      apiResponse <- self$SchedulesScheduleEGetWithHttpInfo(api.key, support.oppose.indicator, max.image.number, max.filing.date, per.page, is.notice, min.dissemination.date, committee.id, sort, candidate.party, last.expenditure.amount, min.amount, last.index, sort.hide.null, max.date, image.number, most.recent, candidate.office.district, min.date, max.amount, line.number, payee.name, last.support.oppose.indicator, candidate.office.state, max.dissemination.date, cycle, last.office.total.ytd, filing.form, min.image.number, candidate.office, sort.nulls.last, candidate.id, sort.null.only, min.filing.date, last.expenditure.date, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    SchedulesScheduleEGetWithHttpInfo = function(api.key, support.oppose.indicator=NULL, max.image.number=NULL, max.filing.date=NULL, per.page=20, is.notice=NULL, min.dissemination.date=NULL, committee.id=NULL, sort='-expenditure_date', candidate.party=NULL, last.expenditure.amount=NULL, min.amount=NULL, last.index=NULL, sort.hide.null=FALSE, max.date=NULL, image.number=NULL, most.recent=NULL, candidate.office.district=NULL, min.date=NULL, max.amount=NULL, line.number=NULL, payee.name=NULL, last.support.oppose.indicator='null', candidate.office.state=NULL, max.dissemination.date=NULL, cycle=NULL, last.office.total.ytd=NULL, filing.form=NULL, min.image.number=NULL, candidate.office=NULL, sort.nulls.last=FALSE, candidate.id=NULL, sort.null.only=FALSE, min.filing.date=NULL, last.expenditure.date=NULL, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['support_oppose_indicator'] <- support.oppose.indicator

      queryParams['max_image_number'] <- max.image.number

      queryParams['max_filing_date'] <- max.filing.date

      queryParams['per_page'] <- per.page

      queryParams['is_notice'] <- is.notice

      queryParams['min_dissemination_date'] <- min.dissemination.date

      queryParams['committee_id'] <- committee.id

      queryParams['sort'] <- sort

      queryParams['candidate_party'] <- candidate.party

      queryParams['last_expenditure_amount'] <- last.expenditure.amount

      queryParams['min_amount'] <- min.amount

      queryParams['last_index'] <- last.index

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['max_date'] <- max.date

      queryParams['image_number'] <- image.number

      queryParams['most_recent'] <- most.recent

      queryParams['candidate_office_district'] <- candidate.office.district

      queryParams['api_key'] <- api.key

      queryParams['min_date'] <- min.date

      queryParams['max_amount'] <- max.amount

      queryParams['line_number'] <- line.number

      queryParams['payee_name'] <- payee.name

      queryParams['last_support_oppose_indicator'] <- last.support.oppose.indicator

      queryParams['candidate_office_state'] <- candidate.office.state

      queryParams['max_dissemination_date'] <- max.dissemination.date

      queryParams['cycle'] <- cycle

      queryParams['last_office_total_ytd'] <- last.office.total.ytd

      queryParams['filing_form'] <- filing.form

      queryParams['min_image_number'] <- min.image.number

      queryParams['candidate_office'] <- candidate.office

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['candidate_id'] <- candidate.id

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['min_filing_date'] <- min.filing.date

      queryParams['last_expenditure_date'] <- last.expenditure.date

      body <- NULL
      urlPath <- "/schedules/schedule_e/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "ScheduleEPage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    },
    SchedulesScheduleETotalsByCandidateGet = function(api.key, sort.hide.null=FALSE, per.page=20, election.full=TRUE, sort.nulls.last=FALSE, candidate.id=NULL, sort.null.only=FALSE, sort='null', cycle=NULL, page=1, ...){
      apiResponse <- self$SchedulesScheduleETotalsByCandidateGetWithHttpInfo(api.key, sort.hide.null, per.page, election.full, sort.nulls.last, candidate.id, sort.null.only, sort, cycle, page, ...)
      resp <- apiResponse$response
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        apiResponse$content
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        apiResponse
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        apiResponse
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        apiResponse
      }
    },

    SchedulesScheduleETotalsByCandidateGetWithHttpInfo = function(api.key, sort.hide.null=FALSE, per.page=20, election.full=TRUE, sort.nulls.last=FALSE, candidate.id=NULL, sort.null.only=FALSE, sort='null', cycle=NULL, page=1, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- c()

      if (missing(`api.key`)) {
        stop("Missing required parameter `api.key`.")
      }

      queryParams['sort_hide_null'] <- sort.hide.null

      queryParams['per_page'] <- per.page

      queryParams['election_full'] <- election.full

      queryParams['sort_nulls_last'] <- sort.nulls.last

      queryParams['candidate_id'] <- candidate.id

      queryParams['sort_null_only'] <- sort.null.only

      queryParams['sort'] <- sort

      queryParams['cycle'] <- cycle

      queryParams['page'] <- page

      queryParams['api_key'] <- api.key

      body <- NULL
      urlPath <- "/schedules/schedule_e/totals/by_candidate/"
      # API key authentication
      if ("X-Api-Key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["X-Api-Key"]) > 0) {
        headerParams['X-Api-Key'] <- paste(unlist(self$apiClient$apiKeys["X-Api-Key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }
      # API key authentication
      if ("api_key" %in% names(self$apiClient$apiKeys) && nchar(self$apiClient$apiKeys["api_key"]) > 0) {
        queryParams['api_key'] <- paste(unlist(self$apiClient$apiKeys["api_key"]), collapse='')
      }

      resp <- self$apiClient$CallApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)

      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        deserializedRespObj <- tryCatch(
          self$apiClient$deserialize(resp, "IETotalsByCandidatePage", loadNamespace("openapi")),
          error = function(e){
             stop("Failed to deserialize response")
          }
        )
        ApiResponse$new(deserializedRespObj, resp)
      } else if (httr::status_code(resp) >= 300 && httr::status_code(resp) <= 399) {
        ApiResponse$new(paste("Server returned " , httr::status_code(resp) , " response status code."), resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        ApiResponse$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        ApiResponse$new("API server error", resp)
      }
    }
  )
)
