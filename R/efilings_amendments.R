# OpenFEC
#
# This application programming interface (API) allows you to explore the way candidates and committees fund their campaigns.    The Federal Election Commission (FEC) API is a RESTful web service supporting full-text and field-specific searches on FEC data. [Bulk downloads](https://www.fec.gov/data/advanced/?tab=bulk-data) are available on the current site. Information is tied to the underlying forms by file ID and image ID. Data is updated nightly.    There are a lot of data, and a good place to start is to use search to find interesting candidates and committees. Then, you can use their IDs to find report or line item details with the other endpoints. If you are interested in individual donors, check out contributor information in the `/schedule_a/` endpoints.    <b class=\"body\" id=\"getting_started_head\">Getting started with the openFEC API</b><br>    If you would like to use the FEC's API programmatically, you can sign up for your own API key using our form. Alternatively, you can still try out our API without an API key by using the web interface and using DEMO_KEY. Note that when you use the openFEC API you are subject to the [Terms of Service](https://github.com/fecgov/FEC/blob/master/TERMS-OF-SERVICE.md) and [Acceptable Use policy](https://github.com/fecgov/FEC/blob/master/ACCEPTABLE-USE-POLICY.md).    Signing up for an API key will enable you to place up to 1,000 calls an hour. Each call is limited to 100 results per page. You can email questions, comments or a request to get a key for 7,200 calls an hour (120 calls per minute) to <a href=\"mailto:APIinfo@fec.gov\">APIinfo@fec.gov</a>. You can also ask questions and discuss the data in a community led [group](https://groups.google.com/forum/#!forum/fec-data).    The model definitions and schema are available at [/swagger](/swagger/). This is useful for making wrappers and exploring the data.    A few restrictions limit the way you can use FEC data. For example, you can’t use contributor lists for commercial purposes or to solicit donations. [Learn more here](https://www.fec.gov/updates/sale-or-use-contributor-information/).    [Inspect our source code](https://github.com/fecgov/openFEC). We welcome issues and pull requests!    <p><br></p> <h2 class=\"title\" id=\"signup_head\">Sign up for an API key</h2> <div id=\"apidatagov_signup\">Loading signup form...</div>
#
# The version of the OpenAPI document: 1.0
# 
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title EfilingsAmendments
#'
#' @description EfilingsAmendments Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field amendment_chain  list( numeric ) [optional]
#'
#' @field depth  numeric [optional]
#'
#' @field file_number  integer [optional]
#'
#' @field last  numeric [optional]
#'
#' @field longest_chain  list( numeric ) [optional]
#'
#' @field most_recent_filing  numeric [optional]
#'
#' @field previous_file_number  numeric [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
EfilingsAmendments <- R6::R6Class(
  'EfilingsAmendments',
  public = list(
    `amendment_chain` = NULL,
    `depth` = NULL,
    `file_number` = NULL,
    `last` = NULL,
    `longest_chain` = NULL,
    `most_recent_filing` = NULL,
    `previous_file_number` = NULL,
    initialize = function(
        `amendment_chain`=NULL, `depth`=NULL, `file_number`=NULL, `last`=NULL, `longest_chain`=NULL, `most_recent_filing`=NULL, `previous_file_number`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!is.null(`amendment_chain`)) {
        stopifnot(is.vector(`amendment_chain`), length(`amendment_chain`) != 0)
        sapply(`amendment_chain`, function(x) stopifnot(is.character(x)))
        self$`amendment_chain` <- `amendment_chain`
      }
      if (!is.null(`depth`)) {
        self$`depth` <- `depth`
      }
      if (!is.null(`file_number`)) {
        stopifnot(is.numeric(`file_number`), length(`file_number`) == 1)
        self$`file_number` <- `file_number`
      }
      if (!is.null(`last`)) {
        self$`last` <- `last`
      }
      if (!is.null(`longest_chain`)) {
        stopifnot(is.vector(`longest_chain`), length(`longest_chain`) != 0)
        sapply(`longest_chain`, function(x) stopifnot(is.character(x)))
        self$`longest_chain` <- `longest_chain`
      }
      if (!is.null(`most_recent_filing`)) {
        self$`most_recent_filing` <- `most_recent_filing`
      }
      if (!is.null(`previous_file_number`)) {
        self$`previous_file_number` <- `previous_file_number`
      }
    },
    toJSON = function() {
      EfilingsAmendmentsObject <- list()
      if (!is.null(self$`amendment_chain`)) {
        EfilingsAmendmentsObject[['amendment_chain']] <-
          self$`amendment_chain`
      }
      if (!is.null(self$`depth`)) {
        EfilingsAmendmentsObject[['depth']] <-
          self$`depth`
      }
      if (!is.null(self$`file_number`)) {
        EfilingsAmendmentsObject[['file_number']] <-
          self$`file_number`
      }
      if (!is.null(self$`last`)) {
        EfilingsAmendmentsObject[['last']] <-
          self$`last`
      }
      if (!is.null(self$`longest_chain`)) {
        EfilingsAmendmentsObject[['longest_chain']] <-
          self$`longest_chain`
      }
      if (!is.null(self$`most_recent_filing`)) {
        EfilingsAmendmentsObject[['most_recent_filing']] <-
          self$`most_recent_filing`
      }
      if (!is.null(self$`previous_file_number`)) {
        EfilingsAmendmentsObject[['previous_file_number']] <-
          self$`previous_file_number`
      }

      EfilingsAmendmentsObject
    },
    fromJSON = function(EfilingsAmendmentsJson) {
      EfilingsAmendmentsObject <- jsonlite::fromJSON(EfilingsAmendmentsJson)
      if (!is.null(EfilingsAmendmentsObject$`amendment_chain`)) {
        self$`amendment_chain` <- ApiClient$new()$deserializeObj(EfilingsAmendmentsObject$`amendment_chain`, "array[numeric]", loadNamespace("openapi"))
      }
      if (!is.null(EfilingsAmendmentsObject$`depth`)) {
        self$`depth` <- EfilingsAmendmentsObject$`depth`
      }
      if (!is.null(EfilingsAmendmentsObject$`file_number`)) {
        self$`file_number` <- EfilingsAmendmentsObject$`file_number`
      }
      if (!is.null(EfilingsAmendmentsObject$`last`)) {
        self$`last` <- EfilingsAmendmentsObject$`last`
      }
      if (!is.null(EfilingsAmendmentsObject$`longest_chain`)) {
        self$`longest_chain` <- ApiClient$new()$deserializeObj(EfilingsAmendmentsObject$`longest_chain`, "array[numeric]", loadNamespace("openapi"))
      }
      if (!is.null(EfilingsAmendmentsObject$`most_recent_filing`)) {
        self$`most_recent_filing` <- EfilingsAmendmentsObject$`most_recent_filing`
      }
      if (!is.null(EfilingsAmendmentsObject$`previous_file_number`)) {
        self$`previous_file_number` <- EfilingsAmendmentsObject$`previous_file_number`
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`amendment_chain`)) {
        sprintf(
        '"amendment_chain":
           [%s]
        ',
        paste(unlist(lapply(self$`amendment_chain`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`depth`)) {
        sprintf(
        '"depth":
          %d
                ',
        self$`depth`
        )},
        if (!is.null(self$`file_number`)) {
        sprintf(
        '"file_number":
          %d
                ',
        self$`file_number`
        )},
        if (!is.null(self$`last`)) {
        sprintf(
        '"last":
          %d
                ',
        self$`last`
        )},
        if (!is.null(self$`longest_chain`)) {
        sprintf(
        '"longest_chain":
           [%s]
        ',
        paste(unlist(lapply(self$`longest_chain`, function(x) paste0('"', x, '"'))), collapse=",")
        )},
        if (!is.null(self$`most_recent_filing`)) {
        sprintf(
        '"most_recent_filing":
          %d
                ',
        self$`most_recent_filing`
        )},
        if (!is.null(self$`previous_file_number`)) {
        sprintf(
        '"previous_file_number":
          %d
                ',
        self$`previous_file_number`
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(EfilingsAmendmentsJson) {
      EfilingsAmendmentsObject <- jsonlite::fromJSON(EfilingsAmendmentsJson)
      self$`amendment_chain` <- ApiClient$new()$deserializeObj(EfilingsAmendmentsObject$`amendment_chain`, "array[numeric]", loadNamespace("openapi"))
      self$`depth` <- EfilingsAmendmentsObject$`depth`
      self$`file_number` <- EfilingsAmendmentsObject$`file_number`
      self$`last` <- EfilingsAmendmentsObject$`last`
      self$`longest_chain` <- ApiClient$new()$deserializeObj(EfilingsAmendmentsObject$`longest_chain`, "array[numeric]", loadNamespace("openapi"))
      self$`most_recent_filing` <- EfilingsAmendmentsObject$`most_recent_filing`
      self$`previous_file_number` <- EfilingsAmendmentsObject$`previous_file_number`
      self
    }
  )
)
